// Package adapterpack implements local lifecycle management for command
// adapter packs. It owns manifest validation, source locks, registry state,
// deterministic test evidence, and runtime resolution helpers; execution
// authority remains in manager/cmdadapter.
package adapterpack
