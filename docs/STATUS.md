# Hideout Implementation Status

<!-- markdownlint-disable MD013 -->

Status source for the current product surface. Detailed contracts remain in the
design documents; this file prevents implementation status from drifting across
many subsystem specs.

<!-- hideout-public-release:start -->
Current release state: public supervised alpha `v0.1.0-alpha.3` for macOS arm64 with
the Lima backend. Source-of-truth identity and receipt digest are in
`releases/current.json`; see the [public release](https://github.com/vibe-agi/hideout/releases/tag/v0.1.0-alpha.3).

This status does not add GA, automatic-update, Linux-package, workspace-DLP,
guest-root-containment, or marketplace-trust claims. Real isolation claims
remain bound to the release's retained Gate 2 and Gate 3 evidence.
<!-- hideout-public-release:end -->

MVP scope: a professional individual operator on their own machine; the
user-facing vocabulary is environments, workspaces, permissions, and audit.

## Implemented Product Paths

| Area | Status |
| --- | --- |
| Lima backend | Reusable environments, workspace mount, identity mounts, helper discovery, and command execution are implemented for the primary macOS dogfood path. Guest tools come from the declared base image or operator-run in-boundary setup; the guest boots from the environment's pinned base image declaration (template or digest-verified URL), and Lima does not run Hideout-owned package provisioning. |
| Lima real-run reference smoke | `scripts/test-lima-real-run.sh` and `scripts/test-phase1.sh --lima-real-run` provide a supervised reference workload over the real Lima backend: deterministic workspace artifact, guest-side success check, selected network evidence, fixed Boundary Action Set, and redacted audit/Boundary Summary checks. This is dogfood evidence for the reference slice, not unattended daily-use, GA, release-candidate, TUI/WebUI, or product-specific agent support. |
| Native backend | Implemented as a weak-isolation development harness only. It is not isolation evidence. |
| Alpha package lifecycle | Implemented. `scripts/package-local.sh` builds the complete host and Linux-helper package, including the observer, pinned package-owned Linux `tun2socks` v2.6.0 helper, provenance manifests, browser assets, schemas, licenses, and runtime contract. The typed installer verifies every declared checksum, records installed state, preserves durable user state, reports obsolete files, and provides explicit repair and uninstall paths. Feature 045 adds a fail-closed clean-tree reproducibility judge and an exact archive-consuming clean-install/alpha.3-upgrade/reinstall/uninstall judge; both passed disposable clean-snapshot implementation validation, but only a later T163/T171 main-tree run may create accepted candidate evidence. The signed and notarized macOS arm64 package identified by `releases/current.json` remains the current public package. The official `vibe-agi/tap/hideout` Homebrew formula consumes exact immutable release bytes, verifies the archive checksum, macOS signature, and package manifest, installs Lima as a dependency, and skips profile initialization. Automatic formula revision updates, Windows, Linux packages, and enterprise deployment remain deferred. |
| Public alpha release channel (033) | Implemented through `v0.1.0-alpha.3`, with immutable receipts retained for all three public alphas. The canonical macOS arm64 package identity separates product version, full source commit, archive digest, and target; publication requires clean source, exact runtime/evidence binding, Developer ID observation, accepted notarization, protected approval, immutable four-asset publication, anonymous digest verification, and a receipt-derived inventory. Apache-2.0 licensing, notices, private vulnerability reporting, issue routing, candidate-neutral docs, and deterministic post-public docs rendering are present. The official Homebrew tap is a distribution view over the same public package and is not a second package truth source. Automatic updates, Linux packages, Windows, and GA support remain out of scope. |
| Community host-app recipes (032) | Implemented and validated with artifact-backed Gate 0 plus a clean exact-package external-pack real macOS arm64 Lima Gate 2. Local or exact-commit Git recipes can bind declarative open-resource commands only to the existing Core provider; they add no JavaScript authority, generic host execution, raw argv, HostFS grant, provider, or marketplace trust. Enablement is profile-scoped and affects future runs only; disabled, revoked, stale, or unowned bindings fail closed without fallback. The canonical clean receipt is part of `.hideout-release-evidence/043-projection-readiness-real-gate2/`. See [host-app-recipes.md](host-app-recipes.md). |
| Doctor/package recovery E2E proof | Implemented as local product-hardening evidence over existing recovery paths. `scripts/test-doctor-package-recovery-e2e.sh --local-fast` reuses package and doctor smoke behavior, then writes schema-valid 024 evidence for stale package verify, repair dry-run, repair apply, repaired verify, durable/unrelated file preservation, doctor deep guidance, safe doctor fix dry-run/apply, selected doctor-report export, and public artifact redaction. It does not add repair authority, install prerequisites, run real gates, or claim release readiness. |
| Test and evidence spine | Implemented. `internal/productevidence` owns the Go registry of required product-hardening proof ids, their feature ownership, layers, freshness policies, artifact policies, and claim ids. Completion helpers for 021-025 now evaluate manifests through that registry; `stale`, missing artifact, and digest mismatch are evaluator results and are not new `ProofEntry.status` values. `hideout support proof-registry --json` exposes the same registry to shell gates and docs truth, and release readiness can include product-hardening evidence only as supporting context that never satisfies real Gate 2/Gate 3 release requirements. |
| Route and event drift guards | Implemented. Manager `/api/v1` route metadata and daemon-specific endpoint metadata are production-owned and tested through shared recognizers rather than test-only route lists. The live-console event catalog names producer/remap behavior, required payload fields, redaction expectation, production source, reducer coverage, and panel coverage; `ValidateEvent` consumes the catalog-required fields. WebUI action-route proof executes served JavaScript with runtime fetch interception and validates observed requests against Manager route recognition, while the compact TUI dashboard is tested to avoid exposing raw HTTP action routes. Gate 0 runs these guards through `scripts/test-live-console-smoke.sh`. |
| Documentation truth gate | Implemented. `docs/claim-boundaries.md` maps current product claims and pending gate requirements to owner docs, proof ids, gates, and non-claims; `docs/command-examples.json` curates safe command checks. `scripts/test-doc-truth-smoke.sh` validates the selected product-hardening proof ids, including all registered 032 ids, scans README/docs/current specs for known overclaim classes, checks curated commands under safe classifications, verifies English README canonicality for localized docs, and writes schema-valid 025 product-hardening evidence. It is a local docs correctness gate, not lifecycle proof, host-effect proof, or release readiness. |
| Recovery codes and hints | Implemented. `internal/recovery` owns the stable `hideout.recovery-codes/v1` registry for selected host-observable failures across package, init, privilege, release-readiness, HostFS, and decisions. `hideout support recovery-codes --json` exposes the same registry to shell gates and docs truth. Doctor human/JSON findings, package CLI diagnostics, init failures, and release-readiness artifacts use registry codes where the v1 surface covers them; Manager/daemon-wide typed error migration and guest bootstrap shell errors remain deferred. |
| Release hardening and compatibility matrix | Implemented. `hideout support matrix` exposes the Go-owned `hideout.support-matrix/v1` alpha support contract (`2026-07-alpha`): macOS arm64 first-class, Linux amd64/aarch64 supported with narrower smoke coverage, native degraded/development-only, DNS/HostFS/privilege claims gate-required, and required non-claims preserved. `hideout version` and `doctor` surface matrix status; `scripts/test-release-readiness.sh` writes redacted `hideout.release-readiness/v1` artifacts and keeps local-fast evidence distinct from release-candidate evidence. Gate 0 includes matrix schemas, docs drift checks, compatibility fixture smoke, and release-hardening smoke. |
| Profile templates and onboarding | Implemented. `hideout init` is template-aware and recommends `privacy` for first run. Built-ins are `privacy`, `hardened`, `dev`, and `debug`; non-interactive onboarding requires explicit profile/template/backend/network choices. `privacy` and `hardened` require `tun2socks`, a proxy secret ref, and a mediated resolver; `hardened` additionally requires enforced privilege separation on a non-native backend that can support product isolation evidence, or fails closed before profile creation unless the operator explicitly creates a visibly degraded fallback. Templates default HostFS name visibility to `none` and enable no adapter packs; an explicit `landmarks` selection expands to one-level discover rules, while `home-tree` requires name-disclosure acknowledgement and categorized sensitive-root exclusions. Successful onboarding writes `profiles/<name>/onboarding-evidence.json` with redacted posture, generated visibility rule IDs, warnings, and non-claims; cancellation, missing choices, unacknowledged broad visibility, existing profile collisions, native-backend enforced hardened claims, and hardened degraded/unknown without explicit fallback write no success evidence. Gate 0 onboarding smoke covers the catalog, degraded honesty, visibility defaults, evidence schema/redaction, and docs commands. |
| External alpha first-run path | Implemented as documentation and local package E2E coverage, not a new authority path. [first-run-alpha.md](first-run-alpha.md) is the canonical walkthrough from package install/verify through a low-prerequisite Lima/direct first success, the distinct privacy follow-up, pinned agent installation, safe `code .`, optional HostFS approval, daemon/TUI/WebUI operator console, and recovery. Gate 0 runs `scripts/test-first-run-docs-smoke.sh` plus `scripts/test-first-run-e2e.sh --local-fast`: the local lane installs with `--skip-init`, verifies the installed package, runs one installed-binary mechanics check, captures audit/Boundary evidence, and writes `hideout.product-hardening-evidence/v1`. Local-fast evidence does not replace the exact-package Lima/direct proof, privacy Gate 3, or release-candidate readiness. |
| Ordinary-user release convergence (044) | The retained 044 ordinary-user journey and its post-`v0.1.0-alpha.2` adversarial hardening were published in `v0.1.0-alpha.3`. Primary/contextual help is concise, `help --all` preserves the advanced inventory, default doctor projects a short ready/attention/blocked answer while verbose/JSON retain full facts, and `hideout support report --out <path>` creates one bounded local-only redacted report. Package smoke proves the package-owned privacy helper and upgrade/repair/uninstall/purge behavior; purge requires exact-store confirmation and all destructive scope is validated before mutation. The package adds fail-closed helper provenance/TOCTOU handling, daemon socket ownership checks, patched toolchains, vulnerability gates, and candidate-bound archive docs. Its exact bytes re-ran clean-package Gate 2, Gate 3, required UI E2E, aggregate readiness, signing, notarization, anonymous redownload, and publication receipt proof. Any later promotion must repeat those gates against its own exact bytes. |
| Operator observability console (045) | Implemented in the current source and gated locally, but not yet published. One declarative task-oriented help catalog feeds CLI, TUI, and WebUI. The Bubble Tea TUI is a persistent HUD with Overview, Activity, Config, Operations, and Help views, progressive drill-down, Enter modals, and read-only STALE/DISCONNECTED states; the browser provides the deeper history view over the same Manager snapshot/events/plans/operations. Profile changes use revision/CAS, canonical plan digests, operation IDs, review/confirm/apply, and desired/effective/transition/evidence separation. Managed secrets use the daemon-owned macOS Keychain path and eligible proxy/DNS changes no longer require daemon or VM restart. Each supported Lima run creates a non-delegated cgroup boundary for the target command and descendants, then records redacted command, file metadata, process-to-IP/port, DNS, coverage, and explainable-risk evidence in an exact-owner private store. Current real-reference coverage is process `Available`, file/network/DNS `Partial` for their documented provider limits; every loss or exit is an interval transition, never a hidden complete claim. T151–T159 model, static/race/mutation, real-Lima, UI/browser, privacy, performance, reproducible-package, and lifecycle judges have passed their recorded development or disposable-clean validation. Final clean main-tree evidence, installed-machine proof, and publication-absence readiness remain T163/T164/T165/T171; no Feature 045 candidate has been remotely published. |
| Zero-friction setup (038) | Implemented in the current source as `hideout setup`: one fixed interactive projection over the same daemon-hosted InitService used by advanced `hideout init`. It reviews Lima, exact `developer-standard` runtime, `/workspace`, shared-default-VM scope (dedicated isolation via `hideout env create`), direct-network non-privacy, hidden outside files, and always-on audit; default rejection writes no profile, while confirmed apply is bound to the reviewed semantic digest and revalidated under the cross-process profile lock. Setup writes configuration only and does not start a VM or download the runtime. The packaged PTY lane proves the local product path; real workspace, identity, runtime, lifecycle, reuse, and pinned-agent claims remain bound to the distinct 038 real macOS arm64 Lima artifacts. Real 038 evidence was regenerated at commit `298db95a5261f7a1cdeb32a3a2f7e0baba761598` with a clean worktree (`dirty:false`); the manifest SHA-256 is `13d54f9aa60ed5981299a916f9467cf931dbe4bfd5d1d67e12d9d01dd9eb6492`, covering the real Lima first run and the separate-session pinned-agent install/run with both install and run logs as distinct artifacts. The retained 022 privacy lane is not replaced. |
| Workspace guard | Implemented before backend prepare; rejects host home, Hideout store, credential roots, and browser profile roots unless explicitly overridden. |
| Daemon-owned concurrent run sessions (034) | Implemented in the current source. Every executable `hideout run` auto-connects to one authenticated per-store daemon role and has no embedded backend fallback; `explain` remains a local non-executable plan. The CLI owns only presentation and its local terminal, the daemon owns Manager/backend/session lifecycle, and the fixed packaged Linux supervisor owns the guest PTY and target process tree. Multiple runs may concurrently reuse the same warm environment and pinned workspace. Each run has a distinct runtime child, broker/data plane, mount and PID namespaces, private `/proc`, terminal stream, and session-scoped HostFS authority. Initial PTY size, SIGWINCH resize, Ctrl-C, full-screen bytes, exact target exit, terminal restoration, daemon-loss target reaping, stop refusal, and explicit fail-closed recovery are covered by the 034 real macOS arm64 Lima lane. The retained result has 26 named isolation/PTY checks and satisfies the 2.0-second warm-attach and 1.25-times pre-034 workspace thresholds; release use still requires clean exact-commit/runtime evidence. The 034 proof alone does not establish 035 cross-workspace sharing, detach, exhaustive terminal-emulator/theme/OSC behavior, workspace-effect isolation, or guest-root containment. Final-session stop is owned by 036. |
| Shared default VM across workspaces (035) | Implemented and promoted for compatible macOS arm64 Lima automatic runs. One canonical profile and machine-compatibility identity selects one stable automatic VM; workspace is session authority, not machine identity. Each run receives one immutable daemon-owned Workspace Portal attachment, one exact logical `/workspace`, and attachment-bound broker/projection/lifecycle state. Shared Lima configuration contains no project mount, and unsupported modes remain explicitly workspace-bound rather than creating a hidden fallback VM. Clean evidence at commit `1584e18f6eb945b69cebe60851b5d3f3bfbfccd8` proved one environment, one instance, and one boot across disjoint projects; same/nested/disjoint authority, sibling detach, restart fail-closed behavior, package/helper identity, redaction, lifecycle generations, 30-sample behavior/performance metrics, and 100-sample saturation all passed. Candidate `git status` median was `49.757 ms` versus `62.208 ms` from the paired static-virtiofs control; warm first-byte p95 was `780.694 ms` versus the retained research baseline of `910.457 ms`. Package SHA-256: `7f6a6e7298163188b48e49747d23941e27a54858019c66b3b8b9038387af886b`; evidence manifest SHA-256: `e4ac83a0c8f3d489ac931479d87e666c24c39ea974a110693a46dd6c39564f13`. Shared projects use one guest kernel and do not receive a VM wall or guest-root containment; use a dedicated named environment for that boundary. Workspace writes remain direct host mutations, and native/unpromoted platforms do not inherit this claim. |
| Resource lifecycle and final-session stop (036) | Implemented and promoted for Lima. One daemon coordinator owns a closed catalog of VM pins, provider drains, retained facts, and independent handoffs for the current observed boot incarnation. Releasing the final pin/drain starts a visible 15-second grace; a new attach cancels it, while unknown inventory, cleanup failure, orphaned ownership, or boot change blocks stop. A successful transaction non-destructively stops the exact instance and retains the environment record, guest disk, profile cache, audit, decisions, and staged HostFS overlay; completed host-app handoffs remain independent and run-scoped bridges close. Clean macOS arm64 evidence at commit `0fe099f20e354d5d52187d9c8d5406a367d19d52` passed all 20 lifecycle checks, 100 attach/stop races, and a 30-sample exact-baseline comparison (`170.511 ms` candidate median versus `271.180 ms` pre-036 baseline). Manifest SHA-256: `b050dfc5c9230bed526132339e7a707a325d352b6ed6baf37c07af09910029fc`. This does not clean/delete retained state, terminate host apps, preserve detached run bridges, claim guest-root containment, or treat native/local evidence as Lima proof. |
| Lifecycle attach reservation (040) | Implemented and promoted for reusable Lima runs. Runs allocate an opaque session ID without filesystem publication, reserve before the environment transition lock, wait older reconciliation without holding that lock, reload/revalidate record and backend facts, materialize runtime, derive shared-workspace authority from the prepared incarnation, write the durable owner, install cleanup, and atomically promote. Reservations block reconcile/stop/forget/idle expiry/destructive mutation, support compatible siblings, abort session-locally, disappear across daemon restart, and expose only redacted counts/stable reason codes. Clean local Gate 0 passed targeted/race suites, 1,000 seeded schedules, mutation proofs, and TLC (530 generated/161 distinct states, depth 14); its manifest SHA-256 is `a0e9dadba70a89ce52e8d5255380ade950ce8e4d8ccb5e7ad96a9cdc21d94d6b`. Clean macOS arm64 Lima evidence at commit `3555c9a9aa83c885c3c8ee29f1d015ee10c1fe73` passed all 24 lifecycle checks, 100 attach/stop races, and 30/30 warm samples within two seconds (`413.921 ms` median, `538.581 ms` nearest-rank p95) against exact pre-040 commit `322c3c6cc9561eea21d4ed20ab78172429654c54` (`408.800 ms` median). Runtime `developer-standard/2026.07.0` remained clean and exact. Real evidence manifest SHA-256: `5394bfbd78804b5c2d1861406861584a5573e25ec7a58edb4f044c2b40fccefb`; retained source-bundle SHA-256: `b934ebaf76a679d90b167d649cea53fcc5c7a659ca02ee12cf060fe9d7e790a9`. Native/local, reduced-probe, prior 036, dirty, and `not-run` evidence still do not satisfy the real claim. |
| Shared Workspace Portal executable support (041) | Implemented and promoted for compatible macOS arm64 Lima automatic/shared sessions at clean candidate `1182fa10bec965cbdecb714faf6f7f9b587221e6`. Linux FUSE `FMODE_EXEC` is accepted only as a local execution hint and is stripped before the unchanged Portal wire request; unknown flags still fail closed. The exact packaged product lane directly executed an interpreted script, Linux arm64 binary, and relative launcher, retained checkout/later-session visibility and negative boundaries, completed 100 executions across two disjoint workspaces, and passed 30 performance samples at 980.621 ms warm first-output p95 and 0.983 median regression ratio. The product manifest SHA-256 is `c84bdaa2a42e16b1bb3e8159d2fcc180590dcbcd3ce7543af70ca1bb8cd9159f`; focused Portal correctness, aggregate Lima Gate 2, and full Gate 0 also passed. The claim remains limited to the shared Workspace Portal: native and static/dedicated virtiofs are unpromoted, with no copied-workspace or host-execution fallback. |
| Disposable orphan recovery (042) | Implemented and promoted for exact `--rm` environments on macOS arm64 Lima at clean candidate `666cfa827646bbc6b0d3d9a86b4f5091b83b5dd3`. Ordinary finalization and daemon restart share one exact-record, exact-instance, owner-refusing, two-sample-absence, record-last protocol; ambiguous facts remain `cleanup-required` without destructive fallback. The verified package SHA-256 is `9f5ba6d168471dbbc1bd6fbdf85809483e4bc1ec1685cd5860427f75ad8d78cd`, bound to clean runtime `developer-standard/2026.07.0` artifact `79e5d25bfd05c27b4ee7f2ad085d45c15a63aadbe2ab8d1b4ba2c426e1586134`. The product lane passed 30/30 ordinary disposals including target exit 23 and `--rm --ephemeral`, four forced daemon-crash/restart cuts, 100 local crash schedules, all 24 closed checks, 34 authorized and zero unauthorized destructive calls, and zero record/journal/backend/gateway/runtime/owner residue. Status p95 was `74.320 ms`; recovery p95 was `498.989 ms`. Product manifest SHA-256: `fc1cabf8eb645433145b72ade45175821c3097792900729c1e9c9231e3dccd16`; recovery artifact SHA-256: `22b708b4560a6ab454c357f4a99f12a921877f758a01774da09cbe1c543e241f`. This is not generic orphan deletion, reusable-environment crash cleanup, guest-root/shared-VM isolation, or authority inferred from names/status/inventory; historical journal-only residue without an exact 042 intent remains explicit recovery. |
| Supported CLI runtime (031) | Implemented. The package-owned catalog contains one retained, digest-pinned `developer-standard` macOS arm64 preview artifact. Core provides pinned profile/environment provenance, actual-guest verification receipts, shared CLI/doctor/Manager status and recovery, probe-only `runtime verify`, non-root user-prefix guidance, package integrity, and typed evidence that independently binds the clean image build and the later verified Hideout package candidate. Completion requires the exact package commit to pass the static battery, all 12 quickstart scenarios, real Gate 2 boundary checks, and real Gate 3 privacy networking plus pinned Codex installation as the non-root target; retained development receipts do not substitute for that clean-package proof. Existing/custom environments remain pinned or `custom/unverified`; selection is explicit. Interactive agent login, automatic refresh/update, Linux amd64 support, published-SBOM supported status, default-runtime selection, patch SLA, and automatic runtime refresh remain outside 031 v1. |
| Command Proxy And Command Adapters | Phase 1 product path implements registered `host.open` command shims using the `open-target-v1` argv schema. Default profiles register `open` and `xdg-open`; profiles may add more host-open command symbols through CLI or Manager/Web typed command-proxy plan/apply without adding new authority. 008 adds local, profile-scoped command capability adapters: digest-pinned JavaScript artifacts or the built-in root-sensitive adapter can own explicit command symbols and return strict Go-validated outcomes (`deny`, `simulate`, `rewriteGuest`, or non-applied `proposeCapability`). 011 adds a local adapter-pack lifecycle: operators can install local directories or exact-commit git sources into a store-wide registry, run deterministic pack tests, and explicitly enable one tested revision per profile with exact pack/revision/adapter/source-digest binding. Adapter enablement uses typed Manager Core plan/apply and CLI; broker routing fails closed on missing, changed, revoked, invalid, or undeclared-command/capability adapters. Root-sensitive adapter evidence remains command-name intent capture and is enriched by 009 privilege status; it does not claim absolute-path, syscall, setuid, or post-guest-root containment. |
| Adapter pack lifecycle | Implemented as a local lifecycle manager, not a public marketplace. `hideout adapter-pack` supports install, upgrade, list, inspect, test, enable, disable, and revoke; Manager `adapter-pack/*` routes manage the same digest-locked pack revisions. Core-owned schema/outcome/command/capability validation is the primary gate; pack-provided tests are mandatory before enablement but are only supporting evidence. Public publisher identity, signing, namespace protection, remote revocation, and marketplace trust remain out of scope until a public marketplace exists. |
| Host open | `host.open` supports external HTTP(S) URLs and workspace-mapped files through a brokered opener, isolated browser profile, local/private URL deny, DNS rebind checks, audit, and Gate 4 coverage. |
| HostFS Portal | `stat`, `read`, and `list` data plane is implemented for Linux guests through FUSE and broker RPC, with grants, reserved-store rejection, filtered list, and audit. Durable profile HostFS allow/deny rules are managed through the same lower-layer rule model from CLI and typed Manager/Web plan/apply. Natural `hideout allow\|deny read\|write\|all <path>` maps existing plain files/directories onto the same Manager profile HostFS planner as profile-scoped rules with an authority-parity test against `profile fs`; `--once` and `--for-this-project` parse but fail closed pending the scope design in [DEBT.md](DEBT.md). |
| HostFS discoverable namespace and exact read approval | Implemented and validated on real macOS arm64 Lima Gate 2. `see`, `see-dir`, and `see-tree` provide explicit per-root name/coarse-kind visibility without content, full metadata, execute, write, or symlink-target authority. Core applies the shared categorized sensitive-root exclusions to presets and manually authored discover rules; discover-denied names stay absent from parent enumeration even when a separately authorized exact content path remains usable. Outside-domain/force-hidden lookup and direct enumeration remain `ENOENT`; visible locked reads return prompt typed `EACCES`; exact-visible directory enumeration returns `EACCES`; incomplete enumeration is explicit; protected-directory failure is typed `EIO`. Eligible exact-file reads create bounded, deduplicated `hostfs.read` decisions. A separate authenticated operator may approve one canonical file for the current live session, after which the same guest retries without restart; owner loss, timeout, deny, symlink retarget, policy drift, malformed state, expiry, and cleanup fail closed. Gate 0 runs local policy/decision/redaction proof, while `scripts/test-hostfs-visibility-e2e.sh --real-gate2` requires all 20 real assertions and records digest-backed `029.hostfs-visibility.real-gate2.namespace` and `029.hostfs-visibility.real-gate2.live-grant`; `not-run` cannot satisfy those claims. The latest adversarial-review rerun records source commit `644e6b53daaa`, `dirty=true`, and Gate 2 log SHA-256 `3b23fc72675c3481d760b6e393fde95122ddc8864e82e0ad9e58bc373f2f7811`: it proves the current worktree behavior but is not clean release provenance. Visible names are user data, hidden predictable paths may still be inferred, and no guest-root containment or workspace filtering is claimed. |
| HostFS write overlay | Implemented for explicit overlay grants. Write-class HostFS operations (`create`, `replace`, `append`, `truncate`, `mkdir`, `delete`, `rename`, `chmod`, constrained `chown`) stage durable session-local overlay records and return guest-visible success only after staging. Host lower files are unchanged until a local authenticated operator claims and applies a Manager decision through CLI/WebUI/TUI/API. Apply revalidates the base snapshot and fails closed on conflicts, stale claims, unsafe paths, or privilege-requiring chown; timeout/discard defaults deny and cleans staged content objects while preserving audit/decision evidence. It does not block workspace writes, provide DLP, contain a guest-root target, or claim to be the only host-mutation path when 009 privilege status is degraded/unknown. |
| HostFS/decision E2E proof | Implemented as product-hardening evidence, not a new authority path. `scripts/test-hostfs-decision-e2e.sh --local-fast` writes schema-valid 023 evidence for staged overlay lifecycle, conflict fail-closed behavior, decision claim/deny/timeout semantics, live-console model visibility, operation coverage-matrix honesty, and public artifact redaction without claiming real guest HostFS data-plane behavior. `--real-gate2` is explicit and prerequisite-gated: it either records `not-run` or proves representative real Lima HostFS staging for file replace plus directory creation through Gate 2. Gate 0 runs the local-fast lane; release or manual real HostFS claims still require real Gate 2 evidence. |
| Profile env policy | Durable `profile.env.public`, `profile.env.inherit`, and `profile.env.deny` are managed from CLI and typed Manager/Web plan/apply. Overview and API responses expose env names only, not public env values. |
| Network | `direct` and guest-side `tun2socks` modes are implemented. The package owns the pinned v2.6.0 Linux helper and release Gate 3 verifies its digest/provenance/license instead of accepting an ambient or operator-supplied binary; the operator still supplies the upstream proxy and mediated resolver. Natural CLI `show connection`, `connect directly`, and `connect through <secret-ref> using <resolver>` use the same revision/CAS Manager transaction as TUI, WebUI, API, and advanced profile commands. A plan states whether the effect is live, next-attach, or blocked. Healthy eligible proxy-upstream and mediated-DNS changes stage, probe, activate, prove, and roll back online without stopping the daemon or recreating the VM; new gateway connections use the activated immutable route while already accepted connections retain their prior binding. VM-global posture changes expose active sibling blockers rather than silently interrupting them. Direct mode retains the last proxy reference/resolver for a later switch but omits inactive proxy material from runtime service configuration. Proxy values come from the daemon-managed secure store and remain absent from target env, service state, process argv, plans, output, and audit. Privacy-mode DNS closure is implemented and validated on real Lima (Gate 3 passes): connected-subnet resolvers are blocked, the guest resolver uses the declared mediated path, and a connected-subnet-only environment is refused. A guest-root target can still rewrite routing/resolver config to restore a bypass (A3 non-claim). |
| Guest privilege separation and risk audit | Implemented for Lima. Hideout provisions a dual identity: target commands run as a non-root target user with target `sudo -n` and `/usr/bin/sudo -n` unavailable, while Go-owned privileged setup/cleanup for network and HostFS uses a separate root-control setup identity. Each run emits `guest.privilege.status` (`enforced`, `degraded`, or `unknown`) and `hideout.privileged_setup`/`hideout.privileged_cleanup` evidence; weak/pre-009 environments where target passwordless sudo still works are marked `degraded` with recreate/base-image guidance and no guest-root containment claim. Real Lima enforced and degraded proof modes are covered by `scripts/test-privilege-separation-smoke.sh`; Gate 3 also asserts `privilege_status=enforced` and `privileged_setup=network`. |
| Tool supply | Implemented as diagnostic-only `tools.expectedCommands` declarations. Hideout ships no package-installation providers, preset-based installers, guest-tool package storage, init/doctor materialization flags, or profile-tool install subcommands. Guest tools come from the declared base image plus operator-authored in-boundary setup run as an ordinary `hideout run`. |
| Manager API | The local token-protected API exposes one authoritative operator snapshot and contiguous event-v2 stream plus profile projections, durable operations, secret metadata/mutation, exact-owner activity summary/events/executions/coverage/risks/export, generic profile transaction plan/apply, compatible typed init/run/network/HostFS/env/command-proxy routes, decisions/notices, and reusable lifecycle controls. Strict request limits, route metadata, local authentication, no-store responses, owner resolution, and stale-client rules apply uniformly. It is not a raw profile writer, secret reader, host execution API, or arbitrary VM control API. |
| TUI operator HUD | Implemented with Bubble Tea v2. `hideout tui` presents one prioritized live screen and five views—Overview, Activity, Config, Operations, and Help—with keyboard navigation, responsive/narrow layouts, filters, session scoping, detail drill-down, and Enter-opened edit/review dialogs. Every mutation follows Draft → Plan → diff/effects/blockers/recovery → Confirm → Apply → terminal Manager evidence; stale, disconnected, expired-credential, and daemonless states are visibly read-only. Secret input is masked and cleared. `--once` is deterministic plain read-only output for scripts and weak terminals. |
| WebUI history console | Implemented as the richer local history/configuration surface over the same Manager domain: overview, session timeline, execution tree, file/network/DNS activity, coverage intervals, risks, operations, compound filters, retained gaps, and canonical configuration dialogs. The event-v2 reducer shares snapshot/gap semantics with TUI; mutation is disabled until an authenticated fresh reseed after disconnect, sequence gap, instance change, or credential rotation. Browser code renders typed Manager data and cannot write profiles, Keychain, VM, or activity storage directly. |
| UI E2E product-hardening proof | Implemented as local evidence, not release readiness. `scripts/test-ui-e2e.sh` writes a redacted `hideout.product-hardening-evidence/v1` manifest with stable proof ids for WebUI browser E2E, TUI PTY E2E, schema, and docs-boundary checks. The browser lane uses local Chrome/Chromium through test-only CDP automation against the daemon-served loopback WebUI, verifies visible console state, live event update without hidden overview/audit polling, notice acknowledgement round trip, wrong-token refusal, and redacted artifacts. The TUI lane uses `script(1)` to run the real `hideout tui` command, verifies visible terminal output, daemon event update, no healthy-stream interval polling, stream fallback, and redacted terminal artifacts. Gate 0 records `not-run` evidence when local browser or PTY prerequisites are absent; targeted completion uses `scripts/test-ui-e2e.sh --all --require-executed --out <dir>`. |
| Endpoint Exposure | Product `endpoint.expose.host-to-guest` is implemented for declared and run-scoped manual guest-loopback TCP candidates, with active owner validation, backend provider, audit, cleanup, and Boundary Summary. |
| Preview open | Minimal `preview.open` is implemented as the first consumer of host-to-guest exposure. Callback adapters, automatic service-listener discovery (`endpoint.observe`), and project-declared auto exposure are later/design-ready; Feature 045's bounded workload process-to-IP/port activity is a separate implemented evidence path. |
| Boundary Summary | Warm `hideout run` startup stays quiet. A Lima start that exceeds one second emits a bounded status line and 30-second heartbeat so image download or boot is not mistaken for a hang. Raw backend output and the Boundary Summary remain behind `--verbose`; `explain`, `hideout audit show`, Manager API, TUI, and Web UI expose the corresponding redacted control-plane evidence. |
| Evidence redaction hygiene | Implemented. Generated machine-id is independent from displayed identity IDs, legacy coupled machine IDs rotate on profile load, and InitTask audit free-text fields pass through the shared deterministic control-plane redaction. |
| `hideoutd` daemon + live operations console | Implemented as the local single-user control plane. `hideout daemon start\|status\|stop` manages one daemon per store; normal executable runs start or reuse it automatically through a separate private `0600` session socket, while Manager HTTP and daemon status/event endpoints remain distinct surfaces. Requests use rotating operator credentials, unauthenticated refusals are audited without token material, and active run credentials/providers remain worker-scoped rather than daemon-global. The daemon owns profile/operation/secret/activity authority, emits typed redacted event-v2 deltas, serves the tokened loopback WebUI, and feeds the TUI reducer. Healthy UI streams do not poll; a gap, instance change, disconnect, or expired credential makes the retained projection STALE/read-only until a fresh authenticated snapshot and stream agree. `--once` may render an explicit daemonless read-only snapshot, but executable `run` never falls back to an embedded Manager/backend. Restart reports ambiguous owners and fails closed rather than re-adopting, replaying, or deleting them. Confirmation is presented by the initiating client and bound to the canonical Manager plan; terminal text, a provider return, or daemon absence never implies success. |
| Export/share redaction boundary | Implemented. `hideout audit export` produces a local shareable artifact from an audit slice, release-evidence bundle, Boundary Summary, or explicitly selected doctor report. Every exported artifact reasserts the Go-owned control-plane strip, resolves referenced local evidence inline or refuses, applies configured `audit.redact` user-data policy with export semantics, preserves the fixed evidentiary set by failing closed on attempted redaction/mutation, writes no partial artifact on failure, emits a local meta-audit summary, and validates through `schemas/export-artifact.schema.json` plus `scripts/test-export-redaction-smoke.sh`. |
| Doctor diagnostics and recovery | Implemented. `hideout doctor` emits local readiness checks as human output or a structured `hideout.doctor-report/v1` JSON report. `--level deep` and `--feature <name>` add structured feature diagnostics for adapters, decisions, packaging, DNS, HostFS, privilege, daemon, export, cleanup, and Lima/backend readiness: observed local facts, candidate causes, next actions, and gate-required markers are explicit, and local doctor never substitutes for real Gate 2/Gate 3 evidence. Human and JSON output render the same findings, reports can be saved with redacted `--evidence-out`, and doctor reports enter export/share only through explicit `--source doctor-report --doctor-report <path>` selection. `doctor --fix --dry-run` previews safe InitTask repairs and `doctor --fix --apply` applies only typed safe repairs with init audit evidence; unsafe recovery remains guidance. Gate 0 includes `schemas/doctor-report.schema.json` and `scripts/test-doctor-smoke.sh` for human/JSON parity, deep and feature-scope diagnostics, required failure, warning exit semantics, control-plane redaction injection, doctor-report export, and dry-run recovery. |
| Operator decision center | Implemented. Actionable decisions (`hostfs.write`, promoted-provider `adapter.proposal` when such a provider exists, and `evidence.share`) are separate from informational notices (`privilege.status`, `background.status`). Decisions use claim/lease/timeout/default-deny semantics and Manager-owned provider dispatch; notices only broadcast and acknowledge facts. Existing HostFS write routes are compatibility shims over the generic decision store, pure local `hideout audit export` remains synchronous, and `hideout audit export --share` creates an `evidence.share` decision whose approval releases the artifact after export redaction checks. CLI (`hideout decision ...`, `hideout notice ...`), Manager API, WebUI, TUI, and daemon events expose redacted records without claim tokens or provider-private refs. Covered by `schemas/decision-record.schema.json`, `schemas/notice-record.schema.json`, and `scripts/test-decision-center-smoke.sh`. |
| Host capability projection (`code .`) and readiness (043) | Implemented and validated on real macOS arm64 Lima. Core owns a static capability registry and generic `host.app.open-resource` provider; exact immutable bindings, constrained grammar, current authority, fixed application roots, and immediate signing checks prevent ambient host execution or shadowed guest fallback. Safe mode isolates editor data and disables extensions/automatic tasks; trusted mode uses a durable, revocable per-profile+workspace grant created with `hideout allow/deny host-app <command>`. The complete built-in plus enabled external catalog is now hashed, published manifest-last, validated in the exact guest session view, and authenticated before lifecycle activation and target commit. A clean exact-package 043 Gate 2 passed 10/10 fresh and 30/30 warm first attempts, one concurrent disjoint-catalog pair, pre-commit cancellation, built-in safe projection, 032 external pack, and 039 separate-run grant/revoke. Fresh and warm readiness p95 remained below the two-second bound; retries, fallback, timeouts, unauthorized host effects, and cross-session access were zero. The canonical proof and exact sample summary are in `.hideout-release-evidence/043-projection-readiness-real-gate2/`. A matching clean Gate 3 was not run, so the newer receipt does not promote alias privacy; the older dirty Gate 3 remains engineering evidence only. adb, AppleScript, and result streaming remain design-ready and fail closed. See [host-capability-projection.md](host-capability-projection.md) and [threat-model.md](threat-model.md). |
| Script runtime | Required Phase 1 supports `command.decide`, `audit.redact`, and 008 command adapter entrypoints through constrained goja ABIs with deterministic time/randomness. `decideCommand(ctx)` continues to govern existing command policy, `redactAudit(ctx)` governs export/user redaction, and `decideCommandAdapter(ctx)` can classify command-name intent and return strict Go-validated adapter outcomes. Bounded context query APIs beyond the supplied command facts are still later work. |

## Not Yet Productized

| Area | Status |
| --- | --- |
| Generic tool provider declarations | Dropped as a product direction: Hideout ships no package-installation providers; tool declarations are thin expected-command diagnostic records. The declarative guest base-environment artifact class (base image references; guest domain, shareable through the ecosystem) is the accepted direction. Imperative environment recipes remain out of scope. |
| `endpoint.expose.guest-to-host` | Lab/separate design. Required before adb, browser DevTools, host service reachability, or similar workflows. |
| Automatic service-listener discovery / AccessSensor | Later. `endpoint.observe` may produce exposure candidates, audit, and warnings, but must not authorize exposure by itself. This is separate from the implemented bounded workload process-to-IP/port activity evidence. |
| Browser control | Lab/separate design. |
| adb / device / simulator workflows | Adapter recipes over future guest-to-host and OpenTarget capabilities; not productized. |
| Additional host app/resource effects | Design-ready only. Community recipes cannot create a new provider; effects beyond `host.app.open-resource` require reviewed Core validators and providers. |
| Public ecosystem | Later. Trust MVP is two-tier: local artifacts, and third-party artifacts behind digest pinning, a permission diff, and one explicit confirmation. Marketplace day-1: signing, revocation/kill-switch, publisher identity, and namespace protection — designed when the marketplace launches, not before. |
