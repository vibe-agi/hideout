# Acceptance And Adversarial Report: Ordinary User Release

<!-- markdownlint-disable MD013 -->

**Feature**: `044-ordinary-user-release`

**Candidate status**: `v0.1.0-alpha.2` remains published and public-verified;
post-publication hardening is local next-candidate work

**Overall status**: Published alpha.2 acceptance remains passed; the next
candidate is not release-ready until the complete exact-package release lane is
rerun

This is the authoritative requirement-to-evidence and adversarial ledger for
the implementation batch. A task is not complete merely because a test is
green: new assertions must have an observed red mutation, new judges must fire
on a negative fixture, and public claims must bind to one retained candidate.
Findings F-043 onward were discovered after alpha.2 publication. Their fixes do
not alter those immutable public bytes and make no alpha.3 publication claim.

## Candidate Identity

| Field | Value | Evidence |
| --- | --- | --- |
| Version | `0.1.0-alpha.2` / `v0.1.0-alpha.2` | immutable GitHub prerelease ID `359952220` |
| Source commit | `9f8f9d81ca7178ccf1e91c6e5a50cf29b3c3b8a5` | exact candidate manifest and public tag |
| Source clean | true | candidate workflow `30278277643` and release manifest |
| Source public/pushed | true | public CI `30277027168` passed before signing |
| Package SHA-256 | `464cf3e96108621c1dc92aef794e83de92c5385086620771da5b47bf86693a76` | retained draft, release API digest, and anonymous redownload agree |
| Manifest SHA-256 | `2c0cdee3d77ffd7238f2e7b356b765d288a28fc967cf60ec5c68578711e3b337` | immutable release asset |
| Signing | accepted | Developer ID Team `YLZ3Y4478K`; candidate/promotion validation passed |
| Notarization | accepted | submission `cd95ea04-a202-4a62-a9d6-016712050b47` |
| Anonymous download | exact match | package and all four public assets matched their API SHA-256 values |
| Publication receipt | `public-verified` | receipt SHA-256 `ca9ef61a9dc9d1e4096126e3c9b2c2e84a6d9c57c93070c8590b3fa5560671c6`; docs truth passed |

## Fresh-Eyes Findings

| ID | Finding | Baseline evidence | Resolution | Status |
| --- | --- | --- | --- | --- |
| F-001 | Top-level help mixes the primary journey with advanced build and laboratory commands. | `hideout help` at pre-044 HEAD | concise primary help plus explicit `help --all` inventory | resolved-local |
| F-002 | `hideout setup --help` is parsed as an invalid setup invocation and exits non-zero. | pre-044 CLI observation | setup help is intercepted before operator-intent parsing and is read-only | resolved-local |
| F-003 | Default doctor renders every passing subsystem and maintainer gate guidance rather than a short user answer. | pre-044 light/deep doctor observation | pure readiness projection; detailed/JSON modes retain all findings | resolved-local |
| F-004 | No single bounded support-report command exists; users must compose doctor/export surfaces themselves. | pre-044 support command inventory | `hideout support report --out <path>` with strict schema, bounded redaction, and safe local write | resolved-local |
| F-005 | Privacy networking requires an operator-installed guest `tun2socks` helper even though it executes inside the product boundary. | `docs/DEBT.md` and package smoke | pinned v2.6.0 package helper, exact upstream license, package/Gate 3 verification, no ambient fallback | resolved-local |
| F-006 | Package lifecycle is implemented but ordinary-user update/uninstall guidance is split across caveats and long-form docs. | help and distribution docs | provider-specific help, preserved-state wording, purge preview/exact-store confirmation, and whole-scope prevalidation | resolved-local |
| F-007 | The release-readiness shell still passed an extracted package root, which cannot prove the outer archive digest accepted by the current readiness model. | `scripts/test-release-readiness.sh` review; `support readiness` rejected the extracted-root identity | release mode now requires the exact archive plus signing/notarization observations and validates clean `HEAD` reachability from `origin/master` | resolved-local |
| F-008 | The older release-dogfood entrypoint still required a maintainer-supplied `HIDEOUT_LINUX_TUN2SOCKS_PATH`, contradicting the package-owned helper journey. | release-dogfood usage and preflight | dogfood now extracts its own retained artifact, verifies it, exports all package-owned helpers, forbids the tun2socks override through release Gate 3, and binds runtime evidence to the archive identity | resolved-local |
| F-009 | Install and first-run smokes still asserted internal detailed findings from default doctor, contradicting the new concise default and causing the first full Gate 0 run to fail after all Go tests and formal models passed. | `scripts/test-install-smoke.sh` exited at `store: ok writable`; first-run used the same stale shape | smokes now assert the concise default surface and separately use `--verbose` for full finding parity; focused first-run lanes and full Gate 0 pass | resolved-local |
| F-010 | The public candidate orchestrator accepted manually supplied prerequisite evidence and did not itself run the 035/036/038/040/041/042 release requirements, allowing stale or separately built evidence to reach readiness. | release registry compared with every manifest appended by `scripts/test-public-alpha-candidate.sh` | candidate orchestration now runs every missing real producer against the exact archive; 036/040 freshness and manifests also bind the exact package | resolved-local |
| F-011 | Gate 3 emitted the required privacy markers and runtime evidence, but no producer promoted them into the registered 030/043 privacy proofs, so complete release readiness was unreachable. | registry, 043 semantic evaluator, Gate 3 output, and candidate orchestration review | strict promotion joins independently executed Gate 2/3 only on matching source/package/runtime artifact, derives the fixed privacy artifact, and passes the production 043 evaluator | resolved-local |
| F-012 | The exact signed candidate's real clean-install preflight created its temporary store with the release operator's ambient `umask`; under the ordinary `022` setting this produced mode `0755`, which the daemon lifecycle journal correctly rejected before first run. | `v0.1.0-alpha.2` workflow run `30192123478`, package SHA-256 `fd75e17f2fd65b5e8256678d1eac10d7c4e41161a696c974b5f282bf047983e4`, followed by `scripts/test-public-alpha-clean-install.sh --real-lima` failing with `store root must be a private directory` | the clean-install fixture now sets its store, synthetic HOME, and Lima control root to `0700` independently of ambient umask; the public-release contract rejects removal of that boundary; the same signed package then completed a real non-root Lima run with a verified runtime cache | resolved-real-preflight-and-final-candidate |
| F-013 | After F-012 was removed, the independent real clean-install preflight still placed the daemon store below macOS's long per-user `TMPDIR`; its authenticated socket path reached 111 bytes and failed before readiness even though only `LIMA_HOME` had a short-root escape hatch. | the same signed package rerun after F-012 reached daemon startup and failed with `socket path too long for this platform (111 bytes)` | the real-Lima fixture now owns a short `hci.*` root under `HIDEOUT_LIMA_SHORT_TMPDIR` (default `/tmp`) for store, HOME, workspace, and daemon socket as well as retaining the separate short Lima home; contract checks reject a return to ambient long `TMPDIR`; `.hideout-release-evidence/044-alpha2-workflow-30192123478/preflight-clean-install-after-f013.json` records the passed real preflight | resolved-real-preflight-and-final-candidate |
| F-014 | The replacement candidate's aggregate runtime Gate 2 reused nested real-gate scripts whose temporary stores still inherited ambient `umask 022`; the first nested image gate reached the daemon with a `0755` store and failed closed. | workflow run `30193969748`, package SHA-256 `ccb44966a931a06cf13229130c9499ade8ea312cdabcb53cc60491affe0a4cf5`, and first full candidate attempt `runtime-gate2.err` | the top-level candidate and standalone runtime real gate now establish `umask 077` for the whole child process tree; the image and drift leaf stores additionally enforce `0700`, and release contracts require all four controls | resolved-local-and-final-candidate |
| F-015 | After the same candidate was rerun with a private caller umask, aggregate Gate 2 reached host-capability projection but macOS Bash 3.2 treated the declared-empty `projection_runtime_args` array as unbound under `set -u`. | second full candidate attempt reached runtime identity and projection setup, then failed at `scripts/lib/gate2-projection.sh:216` with `projection_runtime_args[@]: unbound variable`; cleanup again passed | optional projection runtime arguments now use function positional parameters, whose zero-argument `"$@"` expansion is safe on Bash 3.2; the release contract forbids the old array and requires the new conditional suffix | resolved-local-and-final-candidate |
| F-016 | Once F-014/F-015 were removed, the real projection lane still advertised its host-only SOCKS fixture as `host.lima.internal`; the product gateway correctly rejected that stale guest-facing form because it consumes the secret on the host and publishes a separate guest endpoint. | candidate-like aggregate Gate 2 against workflow `30193969748` crossed retained-image boot, runtime verification, doctor, and the base Lima smoke before failing the projection alias probe with `proxy URL is consumed by the host gateway; use 127.0.0.1 for a proxy running on this host` | the fixture now listens and advertises `127.0.0.1`, leaving host-to-guest endpoint translation solely to the gateway; release contracts require the host form and reject the old `host.lima.internal` form | resolved-local-and-final-candidate |
| F-017 | The exact replacement candidate completed all 33 candidate performance invocations, but the 034 comparison lane then asked the clean pre-034 binary to perform 33 modern attach cycles; its known workspace-dependent machine-fact probe raced the newer shared-session lifecycle and failed on baseline sample 8 before producing comparison evidence. | workflow `30199159639`, package SHA-256 `5043f5e22b99cb9dfa2b5e57d63b0eb1d240ae17e062be34a3e68bddb66c7075`; 30 retained candidate samples and `performance-baseline-8.err` with `runtime observation 6 has invalid frame header`; candidate cleanup receipt passed | investigation removed the repeated historical attach and exposed F-018 rather than suppressing the error or mutating the clean baseline | resolved-investigation |
| F-018 | Once the historical attach failure was removed, the legacy 034 combined `git status`/file-content scan compared Portal p95 with pre-034 static VirtioFS at a 1.25× limit and failed at 8.87×, even though current 034 owns only the absolute host-invocation-to-first-byte criterion and 035 separately owns paired Portal/static-VirtioFS Git and package performance with accepted 2×/3× thresholds. | two-sample real diagnostic: candidate 3486/3521 ms, historical control 347/397 ms; 035 plan, accepted decision, current paired real gate, and semantic evaluator | upgrade 034 performance evidence to v2: 30 real per-run host-invocation-to-first-target-byte samples, representative attached workspace, 2-second p95, and a host monotonic clock; remove the stale pre-034 workload ratio while leaving 035's stricter metric-specific paired gate mandatory | resolved-local-and-final-real-candidate |
| F-019 | The documented 034 quickstart rejected the new valid real isolation artifact before reaching its v2 performance check because it still required exactly 16 boolean checks while the producer and semantic evaluator require 26. | clean `ec755db` real Gate 2 passed with all 26 named checks and 30 performance samples; `scripts/test-concurrent-sessions-quickstart.sh --evidence-dir ... --require-real` stopped at the stale `length == 16` assertion | quickstart now requires the exact sorted 26-name inventory plus all values true, so arbitrary same-count keys and incomplete historical inventories both fail | resolved-local-and-final-clean-candidate |
| F-020 | The exact signed candidate reported a successful safe VS Code handoff, but its run-qualified `--user-data-dir` was deep enough that VS Code's `1.12-main.sock` exceeded Darwin's 103-byte Unix-socket limit; Electron started, then exited with `listen EINVAL`, so 036 could not prove the host application survived VM stop. The same layout leaves only nine bytes of username/path headroom in an ordinary default store. | workflow `30203803506`, package SHA-256 `6161c4b6b15a19d31ddc5eb9cb31f5eddb1578b625741fac9c616be37e05b4df`; full candidate failed with `host application did not survive VM stop`; `.hideout-release-evidence/044-f020-full-lifecycle-probe` then passed the real retained-state/host-handoff, PTY recovery, 100-race, restart, and performance lanes | move run-qualified safe app state to compact store-owned `<store>/ha` while retaining 0700, store-global run isolation, and independence from session cleanup; declare the VS Code IPC suffix/103-byte limit in the strict Core safety profile and reject an overlong effect before launch | resolved-real-probe-and-final-candidate |
| F-021 | The standalone 036 real Gate 2 probe still could not prove F-020 because its explicitly named short-root helper inherited macOS's long per-user `TMPDIR`; the new strict prelaunch guard correctly refused that overlong store. | `.hideout-release-evidence/044-f020-lifecycle-probe/logs/host-app.err` records the bounded refusal below `/var/folders/...`; `.hideout-release-evidence/044-f020-full-lifecycle-probe` passes from the independent short root | default the 036 short root independently to `/tmp`, retain an explicit `HIDEOUT_036_SHORT_TMPDIR` override, and make the release contract reject a return to ambient `TMPDIR` | resolved-real-probe-and-final-candidate |
| F-022 | The 036 paired performance gate mounted one physical Git fixture into the pre-036 static-VirtioFS baseline and the current Portal candidate at the same time. Every AB sample made the following candidate take about 6.1 seconds while every BA candidate sample stayed near 0.53 seconds, so the result included cross-transport workspace interference. | `.hideout-release-evidence/044-f020-performance-probe/logs/performance-{candidate,baseline}-ms.txt` records the deterministic alternating pattern; both new 30-sample independent-workspace probes remove the alternation and retain matching fixture digests | use separate physical candidate/baseline workspaces and require identical tree-and-diff fixture digests before and after measurement; release contract forbids restoring one shared workspace | resolved-real-probe-and-final-candidate |
| F-023 | Even after removing F-022's contamination, the current aggregate gate required every later release to re-pass 036 SC-010 against the pre-036 command path. That attributes the intentional 035 Portal transport and all later security/readiness work to the lifecycle-registry migration, contradicting 036's feature-specific wording and 040's explicit baseline-ownership decision. | 036's promoted clean proof at `0fe099f20e354d5d52187d9c8d5406a367d19d52` measured 170.511 ms versus 271.180 ms at pre-036; `.hideout-release-evidence/044-f020-full-lifecycle-probe` measures current p95 1187.302 ms with 30/30 samples within two seconds against exact pre-040 `322c3c6` | retain `036.lifecycle.real-gate2.performance` as validator-backed supporting historical evidence, keep current 036 lifecycle behavior fresh and exact-package, and make the aggregate current lane emit fresh 040 lifecycle/performance against exact pre-040 `322c3c6`; a new independent 040 semantic validator recomputes sample counts, median, p95, two-second coverage, baseline identity, workspace isolation, commit, package, and runtime | resolved-real-probe-and-final-gate2 |
| F-024 | The replacement signed candidate passed clean install and aggregate real Gate 2, then the runtime mutable-guest drift fixture intermittently failed while changing mode on the newly shadowed `~/.local/bin/git` inode. The claim under test is unprivileged PATH shadowing and runtime drift detection, not guest `chmod` semantics on a profile-home VirtioFS inode. | workflow `30208997784`, package SHA-256 `c8dbcb9dbd5647b555d5a135abc2e65c7c641a431ecd547547558a8b531cd37e`; `.hideout-release-evidence/044-alpha2-exact-candidate-3ef6267/runtime-gate2.err` records `runtime_durable_prefix=passed` followed by `chmod ... Operation not permitted`; `.hideout-release-evidence/044-runtime-drift-diagnostic/run-symlink` then passes the core real Gate 2, durable-prefix, and mutable-drift proofs with the exact signed binary and runtime provenance | shadow the first baseline command with a relative symlink to the already-proven executable that survived stop/start; this remains an unprivileged mutable-PATH drift, removes unrelated post-create chmod ownership timing, and is contract-guarded against restoring the old executable mutation | resolved-real-probe-and-final-candidate |
| F-025 | The next signed candidate completed signing and notarization, but its final Gate 0 package rerun intermittently treated the deliberate PTY master close after native-harness target exit as a product failure. The close is required to keep completion bounded when another process inherits the slave; macOS may wake the concurrent reader with an `os.ErrClosed`-wrapping `PathError`, while the implementation only normalized the equivalent EIO and closed-pipe results. | workflow `30211433185`, job `89818208409` failed `TestRunWithStreamsAllocatesPTYWithInitialSize` with `read /dev/ptmx: file already closed`; the exact test then passed 1000 consecutive local repetitions, and the correction passed another 1000 repetitions, the package race suite, and full Gate 0 including its final native-package rerun | preserve the immediate post-wait close and normalize only errors identifiable through `errors.Is` as EIO, closed pipe, or `os.ErrClosed`; retain all unexpected PTY copy errors and guard both branches with a direct wrapped-error regression test | resolved-local-and-final-candidate |
| F-026 | The exact signed candidate's projection-readiness warm lane intermittently returned a truncated or empty broker response before any host effect. The installed network service owns mutable state below `<service>/network`, but Lima reuse verification and DNS reconfiguration read and rewrote the parent service directory; every healthy warm session was therefore misclassified three times and unnecessarily rebuilt the TUN/DNS path. Removing this defect reduced the race surface but did not alone eliminate F-027, so it is not claimed as the sole root cause. | exact signed package SHA-256 `b18a3dc53bac49f6f76a085882742a93dd7107747b34cf4d3cfe8e82749fd4dc`; `.hideout-release-evidence/044-projection-warm-diagnostic-2/logs/failed-capture/failure-warm-4/session-audit.jsonl` records three failed network checks, successful self-heal, zero gateway connections, no host-app event, and broker EOF; the new path assertion failed against the old command before the fix | keep the helper executable at the service root while all PID/log/resolver/rollback state uses the actual nested network directory; emit bounded counter-only broker transport observations; require zero network verification failures for normal readiness samples and exactly three only in an opt-in forced-self-heal negative fixture | resolved-real-probe-and-final-candidate |
| F-027 | After F-026, another 30-sample real run reached warm 12 with a valid readiness commit but the broker accepted zero connections and the shim decoded a non-JSON response. The reuse verifier checked only TUN/default route/PIDs/DNS; it never revalidated the established `host.lima.internal` control-plane bypass, so a missing or TUN-routed host route could be accepted as healthy and send broker traffic to an unrelated endpoint. | `.hideout-release-evidence/044-normal-network-reuse-diagnostic-9/logs/failed-capture/failure-warm-12/session-audit.jsonl` records successful network setup observations, readiness in 7 ms, broker counters all zero, no host effect, and `invalid character 'H'`; the new bypass assertion failed against the old verifier | re-read every root-owned `local-bypass-*-route.after` identity during warm verification, require a current route for each, and reject any route through `hideout0` before target commit; `.hideout-release-evidence/044-normal-network-reuse-diagnostic-10` then passed 1 fresh + 30 warm + cancellation with every warm at 7–10 ms and all retry/fallback/timeout/effect-leak columns zero | resolved-real-probe-and-final-candidate |
| F-028 | The exact signed candidate verified its package-owned `tun2socks` helper, then Gate 3 copied only the main `hideout` executable into a temporary bin directory. Runtime helper resolution correctly searched beside the executing binary, found no manifest-bound helper there, and failed closed before guest routing. | workflow `30217822392`, exact package SHA-256 `5d0ec17acc6d1cf53d2d3b3048b5e3f4c28d4fc5c750f7b8ef3c634ff4b3f677`; `.hideout-release-evidence/044-alpha2-exact-candidate-2c2ecae/verification/phase1.out` records the failure, while `.hideout-release-evidence/044-f028-gate3-diagnostic-mapped/runtime-gate3/product-hardening-evidence.json` records the same package completing mediated DNS, HTTPS, projection alias privacy, and gateway forwarding with its adjacent helper | release Gate 3 executes the original package binary read-only so `os.Executable` retains helper/manifest adjacency; the public-release contract rejects reintroducing a main-binary-only copy | resolved-real-probe-and-final-candidate |
| F-029 | Gate 4 created its symlink and FIFO inside the host workspace, then passed those host-absolute paths to a target whose visible workspace root is `/workspace`. The broker safely denied both at the guest-root boundary, so the assertions never exercised symlink resolution or special-file rejection and the stale symlink wording hid this false coverage. | `.hideout-release-evidence/044-alpha2-exact-candidate-2c2ecae/verification/phase1.out` stops after `verifying unsafe file target denial`; the bounded rerun reported `is outside workspace`, and `.hideout-release-evidence/044-f029-f030-gate4-diagnostic-2/gate4.out` records the corrected guest-visible fixtures passing the full real-browser lane | pass guest-visible `/workspace/...` paths (including file-URL fixtures), restore the specific `resolves outside workspace` and `not a regular file or directory` assertions, reject host-path fixtures in the release contract, and print bounded denial stderr on mismatch | resolved-real-probe-and-final-candidate |
| F-030 | Standalone Gate 4 inherited macOS's long per-user `TMPDIR`; its daemon socket reached 104 bytes and failed before the browser boundary, even though the public candidate happened to supply a short parent. | direct real-browser rerun failed at `/var/folders/.../hideout-gate4.*/home/.hideout/daemon/hideoutd.sock`; `.hideout-release-evidence/044-f029-f030-gate4-diagnostic-2/gate4.out` records the standalone real-browser lane passing from its owned short root | Gate 4 owns a Darwin `/tmp` default with an explicit absolute non-root override; release-dogfood cleanup inventories both its ambient and short roots | resolved-real-probe-and-final-candidate |
| F-031 | The exact candidate passed real Gate 3, projection alias privacy, gateway forwarding, and real-browser Gate 4, but the retained public `runtime-gate3.out` producer copied the bounded network/agent streams without re-emitting the separately verified alias and gateway markers. The strict privacy promoter correctly refused to infer those claims from the aggregate console log. | workflow `30220894156`, exact package SHA-256 `ffd00cbd0b944fcf5e2167c9346f86860f39300dcc3d282fa1f735f15e998b11`; `.hideout-release-evidence/044-alpha2-exact-candidate-cf7dbd5/verification/phase1.out` contains both `projection_alias_gate3=passed`, `gateway_forward_path=passed`, `gate3: passed`, and `gate4: passed`, while the original digest-bound runtime log omits only the first two and promotion fails closed; `.hideout-release-evidence/044-f031-gate3-diagnostic` reruns the same signed package through real Gate 3 and records both markers in a digest-matching public runtime log | after both checks pass, explicitly emit both bounded markers into the digest-bound public runtime log; keep the promoter strict and add a Gate 0 producer-template contract so a console-only marker cannot recur | resolved-real-probe-and-final-candidate |
| F-032 | The next exact candidate passed clean install but aggregate Gate 2 intermittently failed ordinary sequential projections with broker EOF or timeout; a third candidate-shaped rerun separately captured a lifecycle defect: the 15-second idle timer had already invoked automatic stop, and the next two-phase attach returned `lifecycle stop is in flight` instead of absorbing the sub-second control-plane transition. Removing that direct error did not alone explain all EOF failures. | exact workflow `30223680363`, package SHA-256 `f763dc6f9a7f79835ac0f9dfe6b9a031be0086fe48182ae064bd36eaec2b7c52`, failed wrapper attempts under `.hideout-release-evidence/044-alpha2-exact-candidate-cb62231/{verification,verification-2}`, and `.hideout-release-evidence/044-f032-gate2-candidate-shape-3/gate2.err`; the retained journal commits the matching stop about 311 ms after it starts; after the lifecycle fix one full source Gate 2 passed, but `.hideout-release-evidence/044-f032-fixed-gate2-2` again failed safe projection with EOF | the confirmed lifecycle sub-defect now uses an in-memory completion fence: two-phase reservation waits at most 36 seconds, emits only bounded `automatic-stop-pending`, then takes a fresh backend observation after proved stop; cancellation and unknown stop results remain fail-closed, and the one-shot API keeps stale-observation protection. F-033 makes any pre-effect transport loss observable and safely retryable without repeating an effect. Two post-F-033 full real Gate 2 runs plus one package-shaped full Gate 2 with 1 fresh and 30 warm sessions passed without EOF, timeout, or stop-in-flight. The historical EOF packet origin is intentionally not overclaimed | resolved-real-probe-and-final-candidate |
| F-033 | A projected host-app target proved guest files ready but did not authenticate the guest-to-host broker path before submitting an effect. Transport loss therefore surfaced raw `EOF`/TCP text, while the 20-second client/server request budget was shorter than one allowed 30-second macOS identity step; the launch guard also did not re-check cancellation after slow trust work, allowing a delayed host effect to cross an already-lost response boundary. | F-027 proves a ready first target with broker `accepted=0`; F-032 records residual EOF/timeout. Deterministic red tests showed a guard-cancelled `ExecLauncher` returned success and created its marker, three 45 ms identity steps escaped a 100 ms operation budget, the readiness action was absent, and broker/shim still exposed the old 20-second budget | before any host-app effect, the shim performs a session-token-authenticated, field-empty readiness request and retries only that no-effect probe for at most two seconds. Identity observation now inherits request cancellation and shares one 30-second total; deferred resolution plus final revalidation fit inside nested 65-second operation, 70-second response, and 75-second client budgets. The launcher re-checks cancellation after the guard, and response loss says the app may already be open instead of leaking raw transport text or claiming no effect. Gate 2 requires exact readiness/effect alternation and at least two accepted, parsed, and written broker messages per effect | resolved-real-probe-and-final-candidate |
| F-034 | The exact candidate completed every security and UI lane before ordinary-user setup, but that final fixture nested `hideout-ordinary-user-release.*` and `setup-store-interrupt` below an already long release root. The interrupted setup daemon socket reached 105 bytes and the product correctly refused it on macOS. | final candidate attempt from clean `b54c668`; failure path `/private/tmp/hpa.T07QHR/t/hideout-ordinary-user-release.v2KQwO/setup-store-interrupt/daemon/hideoutd.sock`; all earlier exact-package Gate 2/3/4, lifecycle, first-run, privacy promotion, and forced UI assertions passed | shorten the fixture leaf to `hou.*` and its setup store to `s`; make the public-release contract reject the prior long forms. Local-fast ordinary-user acceptance, a real package setup PTY with a 61-byte interrupted socket, and full Gate 0 then passed | resolved-local-real-setup-and-final-candidate |
| F-035 | After F-034, the exact signed/notarized candidate reached workspace-executable performance after passing clean install, projection readiness, concurrent/shared-workspace/lifecycle gates, and first run. Its direct median remained faster than control (`0.960774x`), but host p95 rose to 2547.459 ms because dozens of historical failed-gate daemons were still alive. Investigation found that once an orphan's deleted runtime reached credential expiry, the rotation worker retried against the same expired deadline every millisecond and appended to an unlinked audit file indefinitely. | workflow `30242909403`, package SHA-256 `761b07e1c5155481794850c9b079da3ad7ac2ef0e5505c9ee697176291c3ddd4`; failed workspace lane measured direct median 1505.7915 ms, control median 1567.269 ms, ratio `0.960774`, and p95 2547.459 ms versus prior exact-candidate p95 944–1664 ms. Three sampled orphan audit descriptors were 4.7, 5.5, and 11.3 GB and growing about 180 KB/s; a bounded 50 ms-TTL reproduction against the exact package added 169,264 bytes in one second, all `daemon.credential.rotate` denials about 1 ms apart | bind credential rotation to the original runtime-directory inode; poll that identity and perform ordered self-stop when the runtime disappears or is replaced; apply 1-second-to-1-minute exponential backoff to other transient rotation failures. Deterministic unit tests cover replacement refusal, runtime-loss self-stop with a one-hour TTL, and single-event retry backoff; an external rebuilt-binary probe emitted one typed runtime-unavailable denial followed by `daemon.stop`. Historical test daemons/SOCKS fixtures and three deleted-root Lima hostagents were precisely removed while retaining the approved release proxy | resolved-local-fault-injection-and-final-candidate |
| F-036 | The clean exact candidate passed every functional, security, privacy, lifecycle, performance, dogfood, and forced-UI lane, but the final ordinary-user evidence scan rejected its own retained `package-ui.out`: `hideout ui --print-url` correctly printed the operator's one-time `ui_...` URL, while the release fixture copied that raw terminal surface into the candidate-public artifact set. | workflow `30248962520`, commit `5e398ed7c797dbca12c55318cf51ce332ea46573`, package SHA-256 `63a84b771b5fd9a083b517ed1bf40d582bd9d54fecc726f1d9ce170bade8efcf`; exact verification passed Phase 1, Gate 2/3/4, projection privacy promotion, UI E2E, and workspace p95 `917.585 ms`, then stopped with `retained evidence contains protected material`; the sole scanner match was `package-ui.out:1` | retain the raw URL only long enough to assert the exact package produced an operator token, stop the daemon to invalidate it, run the installed package's Go-owned `redact-public-evidence` boundary, delete the raw file, assert the retained URL contains `token=REDACTED`, and add a public-release contract guard rejecting direct raw retention | resolved-local-and-final-candidate |
| F-037 | The replacement candidate passed every preceding exact-package lane, including workspace-executable p95 `1213.779 ms`, then the disposable recovery fixture observed `metadata-cleaning` but failed its post-crash assertion because the journal had already been removed. The external watcher wrote a JSON record snapshot before sending `SIGSTOP`, widening the sub-millisecond durable-state window enough for normal product convergence to pass the intended checkpoint. | workflow `30255042654`, commit `74175d03088078104d78bde3bb02a98f5e5e435c`, package SHA-256 `c037190476cadf6c1a806faa318493a1a293b94b6c08cfcbc6ba3e6eec5264b7`; final verification completed 30 ordinary disposable runs and reached checkpoint 3 before `journal.json` was absent. A reduced exact-package probe reproduced immediately; after reordering, three independent real-Lima probes completed planned, backend-absent, and metadata-cleaning recovery, then a full 30-run/all-four-checkpoint probe passed at `.hideout-release-evidence/044-f037-disposable-full-probe` | send `SIGSTOP` immediately after the matching durable state is read and before the watcher writes its own snapshot or marker; retain the same post-stop journal/record assertions, and add a release-contract line-order guard so evidence I/O cannot precede the freeze again | resolved-real-probe-and-final-candidate |
| F-038 | The `f7c82d8` exact candidate passed every product, privacy, lifecycle, recovery, dogfood, and UI lane, but 044's final release-candidate evaluator called a structural helper without `ExpectedCommit`, `ExpectedPackage`, `ExpectedRuntime`, or an artifact root. Once those required trust anchors were supplied, the evaluator correctly exposed that retained Gate 2/3 summaries, the Gate 3 log, and the exact-package TUI capture had bypassed the package-owned public redaction boundary. | workflow `30260756567`, commit `f7c82d8545730b698c3024ba1ae760cd805a0fc4`, package SHA-256 `6ea5934513c7c1da4c5f9a8b60b7b57a8d6f0c2e84fe3e4d72f0b55c035e2bc2`; final verification passed projection readiness at fresh/warm p95 `9/10 ms`, workspace executable p95 `904.32 ms`, 30 ordinary disposable runs plus all four crash checkpoints, Gate 3/4, privacy promotion, CLI dogfood, and forced UI before `validate-044` reported two stale and two artifact-root failures. Re-evaluation with trust anchors then rejected the raw Gate 3 log; a package-redactor scan also found local `auditPath`, runtime evidence, and TUI store paths. | make release/public 044 completion APIs require explicit evaluation options; have the CLI bind the exact manifest package, clean commit, Gate 2 runtime, and sibling artifact root; pass Gate 2/3 summaries, Gate 3 log, TUI capture, and UI launch output through the exact candidate package's public redactor before retention; statically forbid raw retention | resolved-local-and-final-candidate |
| F-039 | The `3871ebf` exact candidate passed every real product lane and the repaired 044 ordinary-user aggregate, but final release readiness rejected 036 lifecycle evidence because the shared lifecycle producer had gained four 040 attach-reservation checks while the strict 036 semantic validator still required the pre-040 20-key inventory. | workflow `30267111123`, commit `3871ebfbff7e77952db94eb986364c9fb7405b66`, package SHA-256 `b30f725b6cb2b88cee2bd245c4d041f8492e18944a82a7518def50ce2af989c4`; exact verification passed clean install, Gate 2/3/4, HostFS, projection readiness/privacy, concurrency, shared workspace, 100 attach/stop races, first run, workspace executable, 30 ordinary disposable runs plus four crash checkpoints, CLI dogfood, forced UI, and all six 044 proofs before readiness reported `036.lifecycle.real-gate2.lifecycle=artifact-invalid(lifecycle real evidence check inventory drifted)`. The retained artifact had all 24 checks true and candidate cleanup retained zero resources. | synchronize the strict 036 lifecycle validator with the current shared 036/040 producer by requiring the four attach-reservation checks, retain exact-inventory rejection for missing or unknown checks, and replay release readiness against the retained exact-package evidence | resolved-local-and-final-candidate |
| F-040 | Public CI for the F-039 fix passed all product changes but failed the cancellation-wait regression because the test slept 25 ms and assumed its ApplyRun goroutine had already entered `ReserveAttach`; the three-core runner could cancel before that scheduling point, turning a cancellation-latency assertion into a scheduler race. | CI run `30274801003` failed only `TestApplyRunCancelledReconciliationWaitPublishesNoRuntime` with `cancelled reconciliation wait did not return`; the same complete Gate 0 passed locally, and the production `ReserveAttach` select already observes `ctx.Done()`. After an explicit entry handshake, 100 normal and 25 race-instrumented repetitions passed. | signal exactly when the test enters `ReserveAttach`, cancel only after that barrier, keep the post-cancel two-second return bound, and retain a separate five-second setup timeout so failure before the tested boundary remains diagnosable | resolved-local-and-public-ci |
| F-041 | The first complete verification of the final `9f8f9d8` candidate reached the same-epoch lifecycle reconcile lane but one backend inventory observation did not complete inside the bounded five-second proof window. | `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/verification` retained `inventory-unavailable`; the product stayed `blocked-unproved` and emitted no unsafe allow. A reduced retry against the same signed package completed in `155.830 ms`, and the complete retry then passed every exact-package lane. | retain the failed observation as a fail-closed operational result; rerun the same immutable candidate without changing bytes or weakening the proof window | resolved-exact-retry; no product bypass |
| F-042 | Promotion published the immutable release and generated receipt-bound docs, but did not publish or retain the release-time Homebrew formula. The generated truth PR therefore failed CI because the official tap still distributed alpha.1, leaving the advertised install command unavailable. | main PR CI `30284899219` attempt 1 failed only `official tap does not distribute the current public release v0.1.0-alpha.2`; tap PR `vibe-agi/homebrew-tap#1` then passed audit/install/test/package-boundary/uninstall workflow `30286163875` and merged as `3a2aeb3`. | publish the exact alpha.2 formula, render `packaging/homebrew/hideout.rb` from the receipt, retain `releases/formulas/v0.1.0-alpha.2.rb`, compare the official tap byte-for-byte with that snapshot in CI, and carry both files in future receipt PRs | resolved-public; DEBT 038 item retired |
| F-043 | Once a package marker was present, a missing, digest-damaged, or wrong-target adjacent `tun2socks` helper could still fall through to a valid mutable store manifest. That silently changed provenance instead of treating package damage as fatal. | The new packaged-damage table initially returned `Source:store-manifest` for all three cases when a valid store helper was available. | Introduce a typed packaged-helper-unavailable error, treat package identity as authoritative, and require an explicit development permission before store resolution is even considered. | resolved-local-next-candidate |
| F-044 | Helper verification and later shim materialization reopened the source path, so a valid helper could be replaced between resolution and copy. The destination bytes were not cryptographically bound to the resolved bytes. | A replacement-after-resolution regression compiled red before the API existed; mutating the final digest condition to `false && ...` made the test report that replaced helper bytes were accepted. | Carry the expected SHA-256 in the resolution and stream one opened, lstat/stat-bound regular executable into a temporary destination while hashing; rename only after the same bytes match. | resolved-local-next-candidate |
| F-045 | The daemon main Unix listener used the platform's path-based unlink-on-close behavior plus unconditional cleanup. If the runtime directory was replaced and a successor bound the same pathname, closing the old listener removed the successor socket. | `TestMainSocketClosePreservesReplacementListener` failed against the old transport with `no such file or directory` after old-listener close. | Disable automatic unlink, remember the bound socket inode, and remove only when the current pathname is still that exact socket. A full Daemon runtime-replacement test also proves self-stop preserves a reachable successor. | resolved-local-next-candidate |
| F-046 | Release builds used Go 1.25.0 and unpatched dependencies, and Gate 0 never ran a vulnerability judge against either source or all final packaged Go binaries. | The published alpha.2 main binary reported Go 1.25.0 and 27 vulnerable symbol findings; the old source scan reported 11 affected vulnerabilities. After the standard-library fix, the first all-binary package scan still rejected `tun2socks` for reachable `GO-2025-3770` through `go-chi/chi v5.2.1`. | Pin Go 1.25.12, `x/text v0.39.0`, `go-chi/chi v5.2.2`, and `govulncheck v1.6.0`; make Gate 0 scan source and the candidate workflow scan every manifest-listed final Go binary; record `goVersion` in candidate metadata. | resolved-local-next-candidate |
| F-047 | Package staging copied repository docs and the Homebrew source formula verbatim. A new candidate archive could therefore describe the previously published version, URL, digest, and “current” status instead of its own candidate/archive identity; the first rendering pass also left its linked first-run guide routing users back to the public Homebrew channel. | Package staging review found alpha.2/alpha.1 release identities in README, localized README, status, distribution guidance, changelog, and formula independent of the requested package version. Follow-up review found the generated README still linked to the copied public-channel first-run guide. | Render publication-neutral package docs before signing, bind nine canonical documents to the dynamic version/tag/archive name, omit predicted public URLs/digests and public-channel install routes, and run a package judge whose per-document stale-identity fixtures all fire. | resolved-local-next-candidate |
| F-048 | Repository truth outside the marked public blocks still called alpha.1 current, the candidate workflow defaulted to alpha.1, the 044 quickstart reused alpha.2 as a future candidate, and promotion notes still claimed every release was the first package. Historical release URLs also lacked receipt-aware validation. | Direct review of distribution/status/workflow/quickstart/promotion text exposed the contradictions while `releases/current.json` already named alpha.2. The prior docs judge inspected current markers but not marker-external current claims or unreceipted history. | Synchronize current truth to alpha.2, next-candidate defaults to alpha.3, make promotion language candidate-neutral, render receipt-backed changelog history, and reject marker-external current drift plus unreceipted historical links while accepting retained alpha.1 history. | resolved-local-next-candidate |
| F-049 | Doctor's local broker health check opened `https://example.com` through a no-op opener, but policy evaluation still performed real DNS. A transient resolver failure therefore produced a safe policy deny and made the TCP broker test look broken. | The final Gate 0 run reached `TestCheckBrokerUsesTCPForLima`, waited the exact two-second resolver budget, and reported only `broker: warn transport=tcp endpoint=present host.open decision=deny status=denied`. | Use an RFC 5737 documentation-only literal IP for the no-effect broker probe so it still traverses the real TCP/request/policy path without DNS; add a poisoned-resolver regression proving zero lookup calls and an allowed host-broker proposal. | resolved-local-next-candidate |
| F-050 | After the reachable-vulnerability fix, verbose scans still reported one root and 21 packaged-`tun2socks` module advisories whose affected packages were absent from the import graph. Twenty had patched releases; `GO-2026-5932` covers only unmaintained `x/crypto/openpgp` packages and has no fixed version. | `govulncheck -show verbose` showed `chi v5.2.2` and `x/crypto v0.38.0` behind the 20 fixable helper findings, plus the no-fix `openpgp` record in both root and helper scans. The scanner's structured trace for `GO-2026-5932` contained only the module frame, while its OSV record named only `golang.org/x/crypto/openpgp` subpackages. | Select `chi v5.3.1` and `x/crypto v0.54.0` in the helper graph and `x/crypto v0.54.0` in root; judge structured symbol scans fail-closed on every imported-package finding and every new/fixable module-only finding. The only exception requires the exact no-fix OSV scope, exact selected module/version, and no package trace; scope change, a fixed release, a downgrade, a forged exception, or any new advisory fails. | resolved-local-next-candidate |

## Requirement Evidence

Evidence values are `missing`, `contradicted`, `weak`, `passed-local`,
`passed-real`, or `passed-public`.

| Requirement group | Required evidence | Current evidence | Status |
| --- | --- | --- | --- |
| FR-001–FR-004 first result | exact-package install/setup/real Lima run | final `9f8f9d8` package passed clean install, setup, first run, and real Lima workspace result | passed-real |
| FR-005–FR-007 help | CLI contract, zero-write checks, inventory mutation | exact-package ordinary-user journey plus focused help contracts and M-002/N-001 | passed-real |
| FR-008–FR-013 doctor/recovery | summary unit/CLI tests, mutation, full-report parity | exact-package first-run/recovery journeys plus focused projection and M-003/N-002 | passed-real |
| FR-014–FR-018 support report | schema, redaction, safe-write, size, negative fixtures | exact-package ordinary-user evidence plus strict schema, redaction, and adversarial fixtures | passed-real |
| FR-019–FR-024 privacy helper | package/helper manifests, license, resolver proof, Gate 3 | exact package supplied the pinned helper and passed real Gate 3 privacy/DNS/proxy proof | passed-real |
| FR-025–FR-027 lifecycle | exact-candidate upgrade/repair/uninstall/purge | exact-package lifecycle journey passed preservation, repair, uninstall, purge, and scope assertions | passed-real |
| FR-028–FR-035 release | aggregate, real gates, UI, signing, notarization, receipt | all six 044 proofs, readiness, signing, notarization, immutable publication, anonymous download, tap install, and receipt-bound docs truth passed | passed-public |

## Requirement And Success-Criterion Audit

This audit distinguishes implemented local behavior from evidence that can only
exist after freezing or publishing one exact candidate.

| IDs | Authoritative evidence reviewed | Observation | Status |
| --- | --- | --- | --- |
| FR-001–FR-004 | clean install, setup PTY, first-run, retained real Lima workspace result from exact `9f8f9d8` package | complete exact-package journey | passed-real |
| FR-005–FR-007 | help unit/CLI inventory, exact-package journey, contextual zero-write checks, M-002/N-001 | complete | passed-real |
| FR-008–FR-013 | doctor projection/full-report tests, exact-package recovery, M-003/N-002 | complete | passed-real |
| FR-014–FR-018 | support-report schema/model/write/CLI tests and exact-package redacted aggregate | complete | passed-real |
| FR-019–FR-024 | pinned helper/license/manifests, exact-package helper provenance, real Gate 3, M-005/N-005 | complete | passed-real |
| FR-025–FR-027 | Homebrew/standalone lifecycle guidance and exact-package preservation/repair/uninstall/purge proof | complete | passed-real |
| FR-028 | exact package ordinary-user journey | all six registered 044 proofs passed against package SHA-256 `464cf3e9…693a76` | passed-real |
| FR-029 | mutation and negative-fixture discipline | M-001–M-020 and N-001–N-017 fired and restored green | passed-local |
| FR-030 | candidate coverage aggregation | exact candidate, Gate 2/3/4, forced UI, and release-readiness aggregate passed | passed-real |
| FR-031 | dirty/private/unpushed/rebuilt/stale/signing/notary/download rejection | N-007 plus readiness/releasechannel judges and immutable anonymous redownload | passed-public |
| FR-032–FR-035 | docs truth, prerelease workflow, debt review, adversarial ledger | receipt-rendered inventory/docs/formula, public docs-truth workflow, and official tap parity passed | passed-public |
| SC-001–SC-002 | supported-Mac first useful result and time bound | exact package passed first result; workspace first byte was `732.522 ms` and all required bounded readiness checks passed | passed-real |
| SC-003–SC-004 | primary/contextual help bounds and coverage | complete | passed-local |
| SC-005–SC-006 | concise healthy/blocked doctor and runnable safe actions | complete | passed-local |
| SC-007 | one bounded schema-valid support artifact with zero injected protected material | complete | passed-real |
| SC-008 | clean-host exact package verification plus real privacy gate | clean install and exact-package real Gate 3 passed | passed-real |
| SC-009 | helper removal/change/replacement/non-executable failure | negative fixtures passed and the exact package helper passed real Gate 3 | passed-real |
| SC-010 | durable/unrelated state preservation through upgrade and normal uninstall | complete | passed-real |
| SC-011 | one exact package across all acceptance, gates, UI, signing, and notary | exact package SHA-256 `464cf3e9…693a76` remained unchanged through publication and anonymous redownload | passed-public |
| SC-012 | all new mutations and judge negative fixtures | complete | passed-local |
| SC-013 | English/Chinese docs, help, package guidance, support matrix, release notes | receipt-rendered docs, official tap formula, and public docs-truth proof passed | passed-public |
| SC-014 | candidate-created resource cleanup | final candidate cleanup retained zero process, Lima, browser, temporary-root, or secret-bearing resources | passed-real |

## Mutation Proofs

| ID | Assertion | Mutation | Red observation | Restored green | Status |
| --- | --- | --- | --- | --- | --- |
| M-001 | Primary next command is exact | Changed `git status --short` to `git status` in `writeSetupNextSteps` | `TestSetupNextStepsKeepsOrdinaryJourneyFirst` failed and printed the changed journey | Restored exact command; focused setup tests passed | passed-local |
| M-002 | Help inventory is complete | Removed the `hideout shim build-linux` entry from expanded help | `TestExpandedHelpRetainsCompleteCommandInventory` failed and named the missing entry | Restored entry; focused help tests passed | passed-local |
| M-003 | Doctor summary reflects readiness state | Changed the attention label from `Needs attention` to `Ready` | `TestDoctorDefaultIsConciseAndDetailedModesRetainFindings` failed and showed the false-ready output | Restored label; focused doctor tests passed | passed-local |
| M-004 | Support redaction rejects protected material | Removed proxy-value matching from the final redaction validator | `TestValidateRejectsOversizeAndProtectedMaterial/proxy` failed because the injected credentialed SOCKS URL was accepted | Restored the proxy validator; support-report package and local-fast acceptance passed | passed-local |
| M-005 | Package helper digest is enforced | Removed the manifest-digest comparison from `Tun2SocksHelperCurrent` | `TestResolveLinuxTun2SocksIgnoresAmbientPathAndRejectsDigestDrift` failed because the drifted store helper resolved | Restored comparison; focused resolver tests and package smoke passed | passed-local |
| M-006 | Rejected uninstall performs no partial deletion | Disabled whole-file-scope prevalidation before the deletion loop | `TestRepairAndUninstallRejectSymlinkedObsoleteAncestors/uninstall` failed because `bin/hideout` had already been removed before the poisoned path was rejected | Restored full prevalidation; focused package tests and recovery E2E passed | passed-local |
| M-007 | Release aggregate rejects mismatched candidate identity | Removed the package-commit-to-checkout-`HEAD` comparison from `validate_release_candidate_source` | `scripts/test-release-readiness.sh --negative-fixtures` failed with `stale package commit fixture unexpectedly passed` | Restored the comparison; the complete negative suite passed | passed-local |
| M-008 | Registry requires every 044 proof | Removed the public-receipt registry row | `TestOrdinaryUserReleaseRegistryCoversTargetedRealAndPublicProofs` failed with `requirements=6 want=7` | Restored row; `go test ./internal/productevidence` passed | passed-local |
| M-009 | Network reuse reads the installed state directory | Evaluated the new exact-path assertions against the previous parent-directory verifier and DNS reconfigure command | `TestEnvironmentNetworkReuseVerificationChecksCurrentBootAndRuntimeHealth` and `TestEnvironmentNetworkDNSReconfigureSeparatesHelperAndStateDirectories` failed, naming the absent nested paths | Corrected the verifier, direct-network stale check, DNS state, and rollback verifier; focused Lima tests passed | passed-local |
| M-010 | Network reuse revalidates every persisted local bypass | Evaluated route-file/current-route assertions against the previous TUN/PID/DNS-only verifier | `TestEnvironmentNetworkReuseVerificationChecksCurrentBootAndRuntimeHealth` failed and named the absent `local-bypass-*-route.after` loop | Added non-empty/current/non-`hideout0` checks; the focused test then passed 100 consecutive repetitions | passed-local |
| M-011 | A cancelled request cannot cross the final host launch boundary | Ran the post-guard cancellation fixture against the launcher before the final context check existed | `TestExecLauncherRefusesEffectWhenGuardConsumesRequestDeadline` returned success and `/usr/bin/touch` created the forbidden marker | Added the final cancellation fence; the test returns `context.Canceled` and the marker remains absent | passed-local |
| M-012 | macOS identity verification consumes one total operation budget | Ran three simulated 45 ms signing/trust steps under a 100 ms budget against the previous per-command timeouts | `TestObserveDarwinSigningIdentityUsesOneBoundedOperationBudget` completed all 135 ms of work and incorrectly succeeded | Bound every command to one inherited operation context; the test now times out within the total budget | passed-local |
| M-013 | Receipt rendering synchronizes the public formula and retains an exact teaching-surface snapshot | Added the formula/snapshot assertions before implementing formula rendering | first-run docs exited 1 because the snapshot was absent, and the Gate 0 identity probe rejected the alpha.1 formula against the alpha.2 inventory | Implemented receipt-bound rendering and the focused docs/public-release contracts passed | passed-local |
| M-014 | Packaged helper damage cannot change provenance | Added missing, digest-damaged, and wrong-target package cases while leaving a valid store helper available | all three cases resolved the store helper instead of rejecting package damage | Package identity now fails closed; the table and explicit development-store permission test pass | passed-local |
| M-015 | Materialized helper bytes equal the resolved digest | Disabled the `CopyVerifiedExecutable` digest mismatch branch after resolving and replacing a helper | the regression failed because replaced bytes reached the destination | Restored the digest fence; the replacement is rejected and no destination remains | passed-local |
| M-016 | Main socket cleanup cannot unlink a successor | Ran the pathname-replacement listener regression against the prior listener cleanup | closing the old listener removed the replacement socket | Inode-owned cleanup preserves the replacement; isolated and full Daemon lifecycle tests pass | passed-local |
| M-017 | The vulnerability gate rejects a reachable known vulnerability | Ran the pinned judge against the fixed vulnerable `x/text v0.38.0` fixture and against the pre-upgrade packaged `tun2socks` | the fixture fired `GO-2026-5970`; the package scan fired reachable `GO-2025-3770` | Source scan and all eight rebuilt package binaries report zero reachable vulnerabilities | passed-local |
| M-018 | Every canonical archive document describes the exact candidate | Injected `v9.9.9-alpha.9` outside the identity marker into each of nine package documents and inspected the linked first-run guide for public-channel routing | the package-doc judge rejected all nine mutations and its channel check rejected the copied public first-run path | Restored generated documents, including a candidate-bound first-run guide; package smoke and identity validation pass | passed-local |
| M-019 | Public documentation truth covers marker-external and historical identities | Added a stale current claim outside markers and an unreceipted historical release URL | both fixtures were rejected; a retained alpha.1 receipt/history fixture remained accepted | Restored docs; public docs truth passes with alpha.2 current and alpha.1 history | passed-local |
| M-020 | Doctor broker health is independent of external DNS | Ran the complete Gate 0 with a transiently unavailable resolver while the probe still named `example.com` | the broker request safely denied after the two-second DNS budget and `TestCheckBrokerUsesTCPForLima` failed | Switched the no-effect probe to an RFC 5737 literal and added a resolver that fails if called; focused repetitions and complete Gate 0 pass | passed-local |
| M-021 | Module-only advisory handling cannot hide imported or new vulnerable code | Fed the structured judge the reachable `GO-2026-5970` fixture, an unexpected module-only ID, and a forged `GO-2026-5932` exception without its authoritative OSV scope | all three were rejected; the pre-upgrade helper independently exposed 20 fixable findings plus the no-fix record | Upgraded selected dependencies and retained only the verified no-fix/unimported `openpgp` scope; source and rebuilt binary scans pass | passed-local |

## Negative Fixtures

| ID | Judge | Fixture | Observed result | Status |
| --- | --- | --- | --- | --- |
| N-001 | Help contextual routing | unknown topic plus an expanded-help fixture with the shim entry removed | CLI rejects the unknown topic with `help --all`; the operator-smoke inventory judge rejects the incomplete fixture | passed-local |
| N-002 | Doctor ordinary-action filter | warning whose sole next action is `scripts/test-gate3-hidden-proxy.sh` | projection remained ready, omitted the finding, and rendered no `scripts/test-*` text; mixed safe/maintainer fixture retained only `hideout daemon start` | passed-local |
| N-003 | Support output validator | symlink, overwrite, unsafe parent, oversize | every fixture was rejected before a final file was retained; atomic success mode is `0600` | passed-local |
| N-004 | Support redaction validator | injected capability/UI token, credentialed proxy, secret backing name, home/workspace path, machine ID, workspace sentinel, and oversize body | unit validators rejected direct injections; the local-fast real CLI report contained none of the injected values or workspace content | passed-local |
| N-005 | Helper provenance validator | missing, digest-damaged, wrong target, invalid release override, invalid development override, and ambient `PATH` helper | installer and Gate 3 validator rejected missing/damaged/wrong-target package helpers; release Gate 3 rejected the override; Go resolver rejected the invalid development override without falling through and ignored ambient `PATH` | passed-local |
| N-006 | Package lifecycle scope | prior install without package helper, unrelated prefix/store files, poisoned out-of-root obsolete path, purge without confirmation | candidate added helper/license while preserving state; repair and uninstall preserved unrelated/durable files; poisoned scope failed before deleting `bin/hideout`; unconfirmed purge failed with store intact | passed-local |
| N-007 | Release candidate validator | dirty checkout, unpushed commit, stale package commit, changed package bytes, stale product digest, missing/failed Gate 2 or Gate 3, native gate substitution, missing 044 UI, invalid signing/notarization, and anonymous-download digest drift | `scripts/test-release-readiness.sh --negative-fixtures` rejected every source fixture and ran the focused releasecompat, releasechannel, and productevidence judges; M-007 proved the stale-commit fixture fires | passed-local |
| N-008 | Projection readiness network reuse judge | remove the actual nested `tun2socks.pid` immediately before one selected warm sample in the disposable gate-owned VM | the selected sample recorded exactly three failed verification attempts, one successful self-heal, one projected first target, one launched host effect, and a successful redacted broker transport observation; all retry/fallback/timeout/effect-leak columns remained zero; the isolated wrapper returned success | passed-real-probe |
| N-009 | Broker readiness no-effect envelope | wrong session token and individually injected subject, command, argv, route, or args fields | the broker denied unauthorized probes, rejected every effect-bearing probe as `bad-request`, and invoked neither host URL nor file opener | passed-local |
| N-010 | Release-time formula and official tap parity | appended an extra teaching-surface line to a copied formula snapshot and independently to a simulated official tap | docs truth rejected the snapshot drift; first-run docs rejected the tap with `official tap differs from the receipt-rendered formula snapshot`; after restoration, focused docs tests and the real official tap comparison passed | passed-local-and-public |
| N-011 | Packaged helper authority | package marker plus missing, changed, or wrong-target adjacent helper and a valid store helper | resolver returns the packaged-helper error and never selects the store; store resolution succeeds only with explicit development permission | passed-local |
| N-012 | Verified helper copy | replace the source inode/bytes after successful resolution | copy rejects the digest mismatch and leaves no destination executable | passed-local |
| N-013 | Daemon socket ownership | rename/recreate the runtime directory and bind a successor at the original main-socket path | old listener close and old daemon self-stop preserve the successor inode and successful dial | passed-local |
| N-014 | Vulnerability judge | fixed vulnerable source fixture plus the original `go-chi/chi v5.2.1` packaged helper | pinned judge exits non-zero for both; patched source and rebuilt final binaries pass | passed-local |
| N-015 | Package documentation identity | inject a foreign candidate into README, Chinese README, status, first-run guide, distribution guide, support matrix, changelog, caveats, and release notes | all nine extracted-archive fixtures are rejected and public-channel routing is absent | passed-local |
| N-016 | Repository release history | stale current claim outside markers, fake history without receipt, and real alpha.1 history with receipt | stale/fake identities are rejected; legitimate historical identity is accepted | passed-local |
| N-017 | DNS-independent doctor broker probe | evaluator resolver always returns an error and counts invocations | documentation-literal probe makes zero resolver calls and retains the allowed host-broker route | passed-local |
| N-018 | Structured vulnerability advisory policy | reachable fixture, unknown module-only finding, forged exception, changed exception scope/fix/version, or any package trace | gate rejects every non-exact case; only the authoritative no-fix `openpgp` OSV record passes while all affected packages are absent from the scan trace | passed-local |

## Verification Commands

Commands are added only after they have actually run. Planned commands live in
`quickstart.md`; this section records observations rather than intentions.

| Command | Candidate identity | Result | Artifact |
| --- | --- | --- | --- |
| `go test ./internal/productevidence` | pre-candidate working tree | passed | local test output, 2026-07-26 |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-scaffold.json` | pre-candidate working tree | scaffold passed | `/tmp/hideout-044-scaffold.json` |
| `go test ./internal/app -run 'Test(PrimaryHelp\|ExpandedHelp\|ContextualHelp\|HelpRejects\|UsageGroups\|SetupRejectsEveryNonHelpArgument)' -count=1` | pre-candidate working tree | passed after observed M-002 red mutation | local test output, 2026-07-26 |
| `go test ./internal/doctor ./internal/app -run 'Test(ReadinessSummary\|DoctorDefaultIsConcise\|DoctorJSONRemainsComplete\|DoctorConciseBlocked)' -count=1` | pre-candidate working tree | passed after observed M-003 red mutation | local test output, 2026-07-26 |
| `go test ./internal/supportreport ./internal/app -count=1` | pre-candidate working tree | passed after observed M-004 red mutation | local test output, 2026-07-26 |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-local-fast.json` | pre-candidate working tree | passed with real CLI redaction/safe-write fixtures | `/tmp/hideout-044-local-fast.json` |
| `go test ./internal/helperbin ./internal/packagekit ./internal/supportreport ./internal/manager ./internal/app` | pre-candidate working tree | passed after replacing the stale external-prerequisite assertion | local test output, 2026-07-26 |
| `scripts/test-package-smoke.sh` | pre-candidate working tree | passed with package-owned helper, license, ambient-path, override, digest, target, install, repair, and uninstall assertions | local test output, 2026-07-26 |
| `cmp third_party/tun2socks/LICENSE <v2.6.0-module>/LICENSE` plus readonly module identity check | pinned `github.com/xjasonlyu/tun2socks/v2@v2.6.0` | exact upstream MIT license matched; module and sums remained pinned | local review output, 2026-07-26 |
| `scripts/test-doctor-package-recovery-e2e.sh --local-fast --out /tmp/hideout-044-recovery-2` | pre-candidate working tree | passed with lifecycle scope, preserved-state, exact purge confirmation, repair, doctor, schema, and redaction evidence | `/tmp/hideout-044-recovery-2/product-hardening-evidence.json` |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-aggregate.json` | pre-candidate working tree | passed complete targeted 044 aggregation and `validate-044 --target targeted-completion` | `/tmp/hideout-044-aggregate.json` plus sibling artifacts |
| `scripts/test-release-readiness.sh --negative-fixtures` | source-fixture repositories plus unit release identities | passed after observed M-007 failure; rejected dirty/private/stale/rebuilt/missing-gate/UI/signing/notarization/download fixtures | transient test roots removed |
| `HIDEOUT_PHASE1_PRINT_PLAN=1 ... scripts/test-release-dogfood.sh` | pre-candidate package `28db82a36da2089a835472ec025c6023129212ef` | passed package-owned helper extraction/verification and release-candidate plan without external tun2socks path | transient `/tmp/hideout-044-dogfood-plan`, removed after review |
| `scripts/test-doc-truth-smoke.sh --out /tmp/hideout-044-doc-truth` | pre-candidate working tree | passed after ordinary-user, helper-supply, candidate, and public-boundary convergence | transient docs evidence |
| `go test ./internal/app ./internal/productevidence ./internal/releasecompat ./internal/releasechannel -count=1` | pre-candidate working tree | passed, including readiness-level refusal of missing 044 UI proof | local test output, 2026-07-26 |
| `go test -race ./internal/supportreport ./internal/helperbin ./internal/packagekit ./internal/productevidence ./internal/releasecompat -count=1` | pre-candidate working tree | passed | local race-test output, 2026-07-26 |
| `scripts/test-install-smoke.sh` plus both local first-run lanes | pre-candidate working tree | passed after F-009 concise/detailed doctor convergence | transient package/evidence roots |
| `scripts/test-gate0.sh` | pre-candidate working tree | passed complete static, unit, formal-model, package, product-smoke, docs, mutation, negative-fixture, first-run, and local UI battery | local console observation, 2026-07-26 |
| `scripts/test-public-alpha-release.sh --no-publish` plus `scripts/test-projection-readiness-smoke.sh` | pre-candidate working tree after F-010/F-011 | passed automatic producer wiring, privacy promotion contract, semantic false-green fixtures, and no-publish authority checks | local test output, 2026-07-26 |
| synthetic matching Gate 2/Gate 3 promotion with distinct environment IDs | retained historical clean 043 fixture plus generated Gate 3 fixture | `validate-043` accepted all seven 030/032/039/043 proofs; mismatched checks remain covered by the Go semantic negative suite | transient `/tmp/hideout-privacy-promote.*`, removed |
| `scripts/test-gate0.sh` | pre-candidate working tree after F-010/F-011 | passed complete Gate 0 including the new privacy promotion, exact-package lifecycle registry, release contract, docs truth, first-run, and UI checks | local console observation, 2026-07-26 |
| shortened real 034 diagnostic with 1 warm-up and 2 measured samples | pre-candidate F-018 working tree | passed three-session isolation, crash/PTY/recovery mechanics, v2 sampling, and cleanup; first-target-byte samples 168.863/182.186 ms | `.hideout-release-evidence/044-f018-probe` (diagnostic only; not release evidence) |
| `go test ./... -count=1`, `scripts/test-public-alpha-release.sh --no-publish`, and docs truth | pre-candidate F-018 working tree | passed strict v2 semantics, complete Go regression, release contracts, and docs truth | local output and ignored docs evidence, 2026-07-26 |
| `scripts/test-gate0.sh` | pre-candidate F-018 working tree | passed full static, unit, formal-model, package, product-smoke, docs, mutation, negative-fixture, first-run, and local UI battery | local console observation, 2026-07-26 |
| `go test -race ./internal/backend/lima ./internal/broker ./internal/manager -count=1`, focused 100-repeat suites, projection and host-capability smokes | pre-candidate F-026/F-027 working tree | passed path, transport-counter, redaction, race, and shell/product-contract regressions | local output, 2026-07-26 |
| readiness-only projection diagnostic, 1 fresh + 30 warm + cancellation | pre-candidate F-027 working tree | passed all 30 warm samples at 7–10 ms with projected first target and zero retry/fallback/timeout/unauthorized/cross-session counts | `.hideout-release-evidence/044-normal-network-reuse-diagnostic-10` (diagnostic only; not release evidence) |
| forced-self-heal readiness-only diagnostic, 1 fresh + 4 warm + cancellation, warm sample 2 injected | pre-candidate F-027 working tree | passed the isolated wrapper; the forced sample completed in 8 ms after exactly three expected verification failures and one self-heal, with no retry/fallback/timeout/effect leak | `.hideout-release-evidence/044-forced-self-heal-diagnostic-11` (diagnostic only; not release evidence) |
| `go test ./... -count=1` and `scripts/test-gate0.sh` | pre-candidate F-027 working tree | passed the complete Go suite and Gate 0 static/unit/race/formal/package/product/docs/first-run/forced-UI battery after the audit schema red test was corrected | local output, 2026-07-27 |
| full source-built runtime Gate 2 with package-equivalent short roots and external host-app pack | dirty F-032 working tree based on `cb622314024fa320d0822f466e8f12cf90252deb` | passed safe/HostFS/trusted/persistent/external projections, complete HostFS, lifecycle, and disposable convergence after the old candidate-shaped run captured `lifecycle stop is in flight` | `.hideout-release-evidence/044-f032-fixed-gate2-1` (diagnostic only; not release evidence) |
| second full source-built runtime Gate 2 with the same shape | same dirty F-032 working tree | failed safe code projection with EOF after the bounded lifecycle wait was present, disproving that the direct stop-in-flight correction resolved every residual transport failure | `.hideout-release-evidence/044-f032-fixed-gate2-2` (diagnostic only; not release evidence) |
| three further full source-built runtime Gate 2 repetitions with retained stores | same dirty F-032 lifecycle working tree before F-033 | all three passed every projection, HostFS, lifecycle, and disposable lane; useful stability diagnostics but not evidence for the later broker/timeout correction | `.hideout-release-evidence/044-f032-residual-gate2-{1,2}` and `.hideout-release-evidence/044-f032-residual-gate2-3.{out,err}` |
| `go test ./... -count=1` plus focused hostcap/appopen/broker/shim/Manager suites | dirty F-033 working tree based on `cb622314024fa320d0822f466e8f12cf90252deb` | passed after the deterministic launch-after-cancel, cumulative identity-budget, missing readiness action, and stale timeout red observations | local output, 2026-07-27 |
| first full source-built runtime Gate 2 after F-033 | same dirty F-033 working tree | passed every projection, HostFS, lifecycle, and disposable lane; retained audits contain one authenticated readiness for every host-app effect and healthy broker transport counters | `.hideout-release-evidence/044-f033-gate2-1.{out,err}` plus retained `/tmp/hd.3iXDgN` |
| second full source-built runtime Gate 2 with the new preflight judge enabled | same dirty F-033 working tree | passed; 13 effect-bearing sessions had equal readiness/effect counts, accepted/parsed/written counts of 2 per pair, and zero parse/write failures; bounded logs contained no EOF, timeout, reset, broken pipe, or stop-in-flight | `.hideout-release-evidence/044-f033-gate2-2.{out,err}` plus retained `/tmp/hd.jiST81` |
| package-shaped full runtime Gate 2 plus projection-readiness stress | package built from the same dirty F-033 working tree | passed the complete Gate 2 and 1 fresh + 30 warm + cancellation; every target was projected, warm readiness averaged 8.8 ms with 12 ms maximum, and retry/fallback/timeout/unauthorized/cross-session counts were all zero | `.hideout-release-evidence/044-f033-readiness-stress` (probe only; not release evidence) |
| `scripts/test-gate0.sh` | dirty F-033 working tree immediately before candidate freeze | passed complete Go, race, seven-model, package, host-app, readiness false-green, support/recovery, release-negative, docs-truth, first-run, UI, concurrency, and lifecycle battery | local console observation, 2026-07-27 |
| exact `scripts/test-public-alpha-candidate.sh --upload-draft` | clean `5e398ed7c797dbca12c55318cf51ce332ea46573`, workflow `30248962520`, package SHA-256 `63a84b771b5fd9a083b517ed1bf40d582bd9d54fecc726f1d9ce170bade8efcf` | clean install, all real Gate 2/3/4 lanes, 30-sample workspace p95 `917.585 ms`, disposable recovery, projection privacy promotion, dogfood, and forced UI passed; final ordinary-user evidence scan correctly rejected the raw retained UI token (F-036) | `.hideout-release-evidence/044-alpha2-exact-candidate-5e398ed/verification` |
| focused public-evidence Go tests, 044 local-fast, `scripts/test-public-alpha-release.sh --no-publish`, and `scripts/test-gate0.sh` | dirty F-036 evidence-boundary fix | all passed; exact-package UI output must cross the Go-owned public redaction boundary and the release contract rejects direct raw retention | local output, 2026-07-27 |
| exact `scripts/test-public-alpha-candidate.sh --upload-draft` | clean `74175d03088078104d78bde3bb02a98f5e5e435c`, workflow `30255042654`, package SHA-256 `c037190476cadf6c1a806faa318493a1a293b94b6c08cfcbc6ba3e6eec5264b7` | clean install through first run passed; projection readiness fresh/warm p95 was `13/9 ms`, workspace executable p95 was `1213.779 ms`, and disposable recovery completed 30 ordinary runs before exposing the checkpoint-watcher ordering defect F-037 | `.hideout-release-evidence/044-alpha2-exact-candidate-74175d0/verification` |
| three reduced real-Lima disposable probes, one 30-run/all-four-checkpoint probe, public no-publish contract, and `scripts/test-gate0.sh` | dirty F-037 gate-only working tree with exact `74175d0` package | all passed after moving `SIGSTOP` before watcher-owned snapshot/marker writes; the full probe retained zero residue and emitted no product proof, while complete Gate 0 passed the static ordering guard and all product/docs/UI/lifecycle regressions | `.hideout-release-evidence/044-f037-disposable-{repro-2,repro-3,repro-4,full-probe}` plus local output, 2026-07-27 |
| exact `scripts/test-public-alpha-candidate.sh --upload-draft` | clean `f7c82d8545730b698c3024ba1ae760cd805a0fc4`, workflow `30260756567`, package SHA-256 `6ea5934513c7c1da4c5f9a8b60b7b57a8d6f0c2e84fe3e4d72f0b55c035e2bc2` | clean install, Gate 2/3/4, HostFS, projection readiness/privacy, concurrency, shared workspace, lifecycle, first run, workspace executable, 30-run/all-four-checkpoint disposable recovery, CLI dogfood, and forced UI all passed; the final ordinary-user evaluator exposed F-038 and cleanup retained zero processes, Lima instances, browsers, temporary roots, or secret-bearing state | `.hideout-release-evidence/044-alpha2-exact-candidate-f7c82d8/verification` |
| focused product-evidence tests, package-redacted replay of the failed exact ordinary-user bundle, `scripts/test-public-alpha-release.sh --no-publish`, and `scripts/test-gate0.sh` | dirty F-038 evidence-validator working tree using the exact `f7c82d8` package redactor | all passed; `validate-044 --target release-candidate` consumed explicit commit/package/runtime/artifact trust anchors, UI retained `token=REDACTED`, Gate 2/3 `auditPath`, Gate 3 runtime evidence, and TUI store paths retained only `<redacted:local-path>`, while complete Gate 0 covered local 044 aggregation, release negative fixtures, docs, UI, lifecycle, and all seven formal models | `.hideout-release-evidence/044-f038-validator-repro` plus local output, 2026-07-27 |
| exact `scripts/test-public-alpha-candidate.sh --upload-draft` plus readiness-only replay | clean `3871ebfbff7e77952db94eb986364c9fb7405b66`, workflow `30267111123`, package SHA-256 `b30f725b6cb2b88cee2bd245c4d041f8492e18944a82a7518def50ce2af989c4`; replay used the dirty F-039 validator fix | every exact product lane and all six 044 proofs passed; readiness first exposed the 036/040 20-versus-24 check-inventory drift, then the fixed strict validator accepted the same retained package/runtime/Gate 2/Gate 3 evidence with `releaseReady=true` and cleanup all zero | `.hideout-release-evidence/044-alpha2-exact-candidate-3871ebf` |
| `scripts/test-gate0.sh`, public CI, then 100 normal plus 25 race-instrumented focused repetitions | local Gate 0 on `e1555db` passed; public CI `30274801003` exposed the pre-entry scheduler race; focused repetitions used the dirty F-040 test fix | the product cancellation path remained unchanged; the repaired test now proves it entered the reconciliation wait before cancellation and every repetition returned within the original two-second bound | local and GitHub Actions output, 2026-07-27 |
| public CI plus protected exact candidate workflow | clean public `9f8f9d81ca7178ccf1e91c6e5a50cf29b3c3b8a5`; CI `30277027168`; candidate workflow `30278277643` | source CI passed; the protected workflow built, signed, notarized, validated, and retained one private draft package with SHA-256 `464cf3e96108621c1dc92aef794e83de92c5385086620771da5b47bf86693a76` | `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/workflow` |
| complete same-package verification retry after F-041 | exact signed/notarized `9f8f9d8` package; no rebuild | clean install, HostFS, projection readiness/privacy, concurrent/shared workspace, 100 attach/stop races, first run, executable workspace, 30 disposable runs plus four crash checkpoints, Gate 2/3/4, CLI dogfood, forced TUI/WebUI, all six 044 proofs, and release readiness passed; cleanup retained zero resources | `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/verification-retry-1` |
| protected public-alpha promotion and anonymous redownload | workflow `30284678443`, release `v0.1.0-alpha.2`, release ID `359952220` | immutable prerelease published from commit `9f8f9d8`; all four API/download digests matched, receipt status was `public-verified`, and the independently downloaded package matched SHA-256 `464cf3e96108621c1dc92aef794e83de92c5385086620771da5b47bf86693a76` | release URL plus `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/{publication-workflow,public-anonymous}` |
| official Homebrew tap PR audit/install/test/package-boundary/uninstall | tap PR `vibe-agi/homebrew-tap#1`, workflow `30286163875`, merge `3a2aeb3dbf2b180d2ea16aa892f1d1b0cc013434` | all three tap jobs passed and the public formula now distributes the exact alpha.2 URL, digest, helper set, and caveats | public tap main plus retained publish checkout |
| receipt-bound truth PR CI and post-public truth | PR `vibe-agi/hideout#2`; PR CI `30284899219` attempt 2; merged master CI `30287485324`; docs-truth workflow `30287484122` | both Gate 0 runs passed against the official alpha.2 tap; merged inventory/receipt/docs passed both `033.release.docs-public-truth` and `033.release.public-download` | `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/{docs-truth-workflow,docs-truth-local}` |
| formula rendering contracts and drift mutations | post-public source based on master `4e8191e`; exact alpha.2 inventory/receipt | formula identity and missing-snapshot assertions first failed; receipt rendering, idempotence, snapshot drift rejection, simulated tap drift rejection, public no-publish rehearsal, and focused docs truth then passed | `.hideout-release-evidence/044-alpha2-exact-candidate-9f8f9d8/{formula-contract-docs-local,tap-drift-mutation.out}` plus local output |
| final post-public Gate 0 | post-public formula-closure working tree based on master `4e8191e`; exact official alpha.2 tap formula required | complete Gate 0 passed, including formula/tap parity, public truth, install, first-run, lifecycle, concurrency, and all formal-model lanes | local console observation, 2026-07-28 |
| `go test ./internal/helperbin ./internal/manager -count=1` | post-public next-candidate working tree | passed packaged-damage/store-permission and replacement-after-resolution copy tests; Manager materialization uses the verified-copy path | local output, 2026-07-28 |
| focused `go test ./internal/daemon` main-socket/runtime-replacement tests | post-public next-candidate working tree | passed isolated listener and full Daemon successor-socket preservation | local output, 2026-07-28 |
| `scripts/test-vulnerability-gate.sh --self-test --source` | Go 1.25.12 next-candidate working tree with `x/crypto v0.54.0` | vulnerable fixture fired `GO-2026-5970`; unknown module-only and forged-exception fixtures were rejected; source had zero imported-package or fixable module findings, with the no-fix `GO-2026-5932` trace confined to unimported `openpgp` packages | local output, 2026-07-28 |
| two-phase local package build plus `scripts/test-vulnerability-gate.sh --package-root <stage>/hideout` | Go 1.25.12 next-candidate package | first run rejected reachable `GO-2025-3770` in packaged `tun2socks`; after selecting `chi v5.3.1` and `x/crypto v0.54.0`, all final Go binaries passed the structured imported-package/module-only policy | isolated temporary package roots, 2026-07-28 |
| `scripts/test-package-smoke.sh` | post-public next-candidate working tree | package manifest/lifecycle passed; candidate-bound docs matched the dynamic archive and all nine stale-document fixtures fired | transient package root removed, 2026-07-28 |
| `scripts/test-doc-truth-smoke.sh` | alpha.2 current inventory plus next-candidate source | passed marker-external current drift, unreceipted history, and retained alpha.1 history fixtures | transient docs evidence, 2026-07-28 |
| `go test ./... -count=1`, focused race tests, and all four `go mod verify` checks | post-review next-candidate working tree | complete Go regression, helper/daemon/Manager race coverage, and root/tool module integrity passed | local output, 2026-07-28 |
| 100 focused doctor-broker repetitions plus 10 race-instrumented repetitions | DNS-independent doctor probe fix after the complete Gate 0 resolver failure | every run retained the real TCP/policy path, made zero calls to the poisoned resolver, and passed | local output, 2026-07-28 |
| `scripts/test-gate0.sh` with `HIDEOUT_TAP_FORMULA` bound to a fresh read-only download of the official tap formula | post-review next-candidate working tree; official tap alpha.2 formula matched the receipt snapshot before execution | complete Gate 0 passed source vulnerability, all seven models, package/install/docs negatives, runtime, daemon, workspace, HostFS, release truth, first-run, UI, concurrency, and lifecycle lanes | local console observation, 2026-07-28 |
| rebuilt alpha.3 developer-preview package, structured scan of all eight manifest-listed Go binaries, full Go/race regressions, package smoke, and a fresh official-tap-bound `scripts/test-gate0.sh` | post-F-050 working tree with root/helper `x/crypto v0.54.0` and helper `chi v5.3.1` | every final binary used Go 1.25.12; no imported-package or fixable module finding remained; only the exact no-fix/unimported `GO-2026-5932` scope was accepted for the main and helper binaries; complete Gate 0 passed | transient package roots removed and local console observation, 2026-07-28 |

## Cleanup Inventory

| Resource class | Created | Removed/accounted | Status |
| --- | --- | --- | --- |
| Test sessions | exact-package Gate 2/3/4, first-run, lifecycle, UI, and disposable fixtures | final candidate cleanup reported zero retained candidate-path processes | passed-real |
| Test environments | gate-owned Lima environments across all exact-package lanes | cleanup reported zero retained candidate Lima processes; diagnostic Lima instances were stopped/deleted | passed-real |
| Temporary stores | clean-install, lifecycle, workspace, disposable, UI, and diagnostic stores | candidate cleanup reported zero retained roots; the three explicitly retained F-041 diagnostic roots were moved to Trash after review | passed-real |
| Package prefixes | clean install, upgrade/repair, standalone lifecycle, and Homebrew tap CI prefixes | gate cleanup and tap uninstall removed all test prefixes | passed-real-and-public |
| Support reports | local-fast and exact-package temporary reports | test roots removed; no user-store report residue | passed-real |
| Local proxy | approved `127.0.0.1:60123` release fixture | exact PID `17512` stopped after Gate 3/public verification; its temporary root was moved to Trash | passed-real |
| Retained release evidence | exact candidate, workflow, anonymous download, receipt, docs truth, and mutation artifacts | explicitly retained only under ignored `.hideout-release-evidence/`; no secret-bearing state retained | passed-public |

## Completion Audit

044 may be marked implemented only when:

- every task is checked with linked evidence;
- every functional requirement and success criterion is passed at its required
  local, real, or public scope;
- all mutation and negative-fixture rows are green;
- exact-package Gate 2, Gate 3, and required UI evidence pass;
- the clean commit is public and CI is green;
- signing, notarization, anonymous download, and publication receipt validate;
- documentation and release inventory are rendered from the receipt; and
- cleanup has no unaccounted residue.
