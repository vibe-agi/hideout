# Acceptance And Adversarial Report: Ordinary User Release

<!-- markdownlint-disable MD013 -->

**Feature**: `044-ordinary-user-release`

**Candidate status**: Not frozen

**Overall status**: In progress

This is the authoritative requirement-to-evidence and adversarial ledger for
the implementation batch. A task is not complete merely because a test is
green: new assertions must have an observed red mutation, new judges must fire
on a negative fixture, and public claims must bind to one retained candidate.

## Candidate Identity

| Field | Value | Evidence |
| --- | --- | --- |
| Version | not frozen | pending release decision |
| Source commit | not frozen | pending clean public commit |
| Source clean | not proved | pending candidate freeze |
| Source public/pushed | not proved | pending push and CI |
| Package SHA-256 | not built | pending retained package |
| Manifest SHA-256 | not built | pending retained package |
| Signing | not run | protected release workflow |
| Notarization | not run | protected release workflow |
| Anonymous download | not run | public release |
| Publication receipt | not run | public release |

## Fresh-Eyes Findings

| ID | Finding | Baseline evidence | Resolution | Status |
| --- | --- | --- | --- | --- |
| F-001 | Top-level help mixes the primary journey with advanced build and laboratory commands. | `hideout help` at pre-044 HEAD | concise primary help plus explicit `help --all` inventory | resolved-local |
| F-002 | `hideout setup --help` is parsed as an invalid setup invocation and exits non-zero. | pre-044 CLI observation | setup help is intercepted before operator-intent parsing and is read-only | resolved-local |
| F-003 | Default doctor renders every passing subsystem and maintainer gate guidance rather than a short user answer. | pre-044 light/deep doctor observation | pure readiness projection; detailed/JSON modes retain all findings | resolved-local |
| F-004 | No single bounded support-report command exists; users must compose doctor/export surfaces themselves. | pre-044 support command inventory | `hideout support report --out <path>` with strict schema, bounded redaction, and safe local write | resolved-local |
| F-005 | Privacy networking requires an operator-installed guest `tun2socks` helper even though it executes inside the product boundary. | `docs/DEBT.md` and package smoke | pinned v2.6.0 package helper, exact upstream license, package/Gate 3 verification, no ambient fallback | resolved-local |
| F-006 | Package lifecycle is implemented but ordinary-user update/uninstall guidance is split across caveats and long-form docs. | help and distribution docs | provider-specific help, preserved-state wording, purge preview/exact-store confirmation, and whole-scope prevalidation | resolved-local |
| F-007 | The release-readiness shell still passed an extracted package root, which cannot prove the outer archive digest accepted by the current readiness model. | `scripts/test-release-readiness.sh` review; `support readiness` rejected the extracted-root identity | release mode now requires the exact archive plus signing/notarization observations and validates clean `HEAD` reachability from `origin/master` | resolved-local |
| F-008 | The older release-dogfood entrypoint still required a maintainer-supplied `HIDEOUT_LINUX_TUN2SOCKS_PATH`, contradicting the package-owned helper journey. | release-dogfood usage and preflight | dogfood now extracts its own retained artifact, verifies it, exports all package-owned helpers, forbids the tun2socks override through release Gate 3, and binds runtime evidence to the archive identity | resolved-local |
| F-009 | Install and first-run smokes still asserted internal detailed findings from default doctor, contradicting the new concise default and causing the first full Gate 0 run to fail after all Go tests and formal models passed. | `scripts/test-install-smoke.sh` exited at `store: ok writable`; first-run used the same stale shape | smokes now assert the concise default surface and separately use `--verbose` for full finding parity; focused first-run lanes and full Gate 0 pass | resolved-local |
| F-010 | The public candidate orchestrator accepted manually supplied prerequisite evidence and did not itself run the 035/036/038/040/041/042 release requirements, allowing stale or separately built evidence to reach readiness. | release registry compared with every manifest appended by `scripts/test-public-alpha-candidate.sh` | candidate orchestration now runs every missing real producer against the exact archive; 036/040 freshness and manifests also bind the exact package | resolved-local |
| F-011 | Gate 3 emitted the required privacy markers and runtime evidence, but no producer promoted them into the registered 030/043 privacy proofs, so complete release readiness was unreachable. | registry, 043 semantic evaluator, Gate 3 output, and candidate orchestration review | strict promotion joins independently executed Gate 2/3 only on matching source/package/runtime artifact, derives the fixed privacy artifact, and passes the production 043 evaluator | resolved-local |
| F-012 | The exact signed candidate's real clean-install preflight created its temporary store with the release operator's ambient `umask`; under the ordinary `022` setting this produced mode `0755`, which the daemon lifecycle journal correctly rejected before first run. | `v0.1.0-alpha.2` workflow run `30192123478`, package SHA-256 `fd75e17f2fd65b5e8256678d1eac10d7c4e41161a696c974b5f282bf047983e4`, followed by `scripts/test-public-alpha-clean-install.sh --real-lima` failing with `store root must be a private directory` | the clean-install fixture now sets its store, synthetic HOME, and Lima control root to `0700` independently of ambient umask; the public-release contract rejects removal of that boundary; the same signed package then completed a real non-root Lima run with a verified runtime cache | resolved-real-preflight; exact replacement candidate pending |
| F-013 | After F-012 was removed, the independent real clean-install preflight still placed the daemon store below macOS's long per-user `TMPDIR`; its authenticated socket path reached 111 bytes and failed before readiness even though only `LIMA_HOME` had a short-root escape hatch. | the same signed package rerun after F-012 reached daemon startup and failed with `socket path too long for this platform (111 bytes)` | the real-Lima fixture now owns a short `hci.*` root under `HIDEOUT_LIMA_SHORT_TMPDIR` (default `/tmp`) for store, HOME, workspace, and daemon socket as well as retaining the separate short Lima home; contract checks reject a return to ambient long `TMPDIR`; `.hideout-release-evidence/044-alpha2-workflow-30192123478/preflight-clean-install-after-f013.json` records the passed real preflight | resolved-real-preflight; exact replacement candidate pending |
| F-014 | The replacement candidate's aggregate runtime Gate 2 reused nested real-gate scripts whose temporary stores still inherited ambient `umask 022`; the first nested image gate reached the daemon with a `0755` store and failed closed. | workflow run `30193969748`, package SHA-256 `ccb44966a931a06cf13229130c9499ade8ea312cdabcb53cc60491affe0a4cf5`, and first full candidate attempt `runtime-gate2.err` | the top-level candidate and standalone runtime real gate now establish `umask 077` for the whole child process tree; the image and drift leaf stores additionally enforce `0700`, and release contracts require all four controls | resolved-local; exact replacement candidate pending |
| F-015 | After the same candidate was rerun with a private caller umask, aggregate Gate 2 reached host-capability projection but macOS Bash 3.2 treated the declared-empty `projection_runtime_args` array as unbound under `set -u`. | second full candidate attempt reached runtime identity and projection setup, then failed at `scripts/lib/gate2-projection.sh:216` with `projection_runtime_args[@]: unbound variable`; cleanup again passed | optional projection runtime arguments now use function positional parameters, whose zero-argument `"$@"` expansion is safe on Bash 3.2; the release contract forbids the old array and requires the new conditional suffix | resolved-local; exact replacement candidate pending |
| F-016 | Once F-014/F-015 were removed, the real projection lane still advertised its host-only SOCKS fixture as `host.lima.internal`; the product gateway correctly rejected that stale guest-facing form because it consumes the secret on the host and publishes a separate guest endpoint. | candidate-like aggregate Gate 2 against workflow `30193969748` crossed retained-image boot, runtime verification, doctor, and the base Lima smoke before failing the projection alias probe with `proxy URL is consumed by the host gateway; use 127.0.0.1 for a proxy running on this host` | the fixture now listens and advertises `127.0.0.1`, leaving host-to-guest endpoint translation solely to the gateway; release contracts require the host form and reject the old `host.lima.internal` form | resolved-local; exact replacement candidate pending |
| F-017 | The exact replacement candidate completed all 33 candidate performance invocations, but the 034 comparison lane then asked the clean pre-034 binary to perform 33 modern attach cycles; its known workspace-dependent machine-fact probe raced the newer shared-session lifecycle and failed on baseline sample 8 before producing comparison evidence. | workflow `30199159639`, package SHA-256 `5043f5e22b99cb9dfa2b5e57d63b0eb1d240ae17e062be34a3e68bddb66c7075`; 30 retained candidate samples and `performance-baseline-8.err` with `runtime observation 6 has invalid frame header`; candidate cleanup receipt passed | investigation removed the repeated historical attach and exposed F-018 rather than suppressing the error or mutating the clean baseline | resolved-investigation |
| F-018 | Once the historical attach failure was removed, the legacy 034 combined `git status`/file-content scan compared Portal p95 with pre-034 static VirtioFS at a 1.25× limit and failed at 8.87×, even though current 034 owns only the absolute host-invocation-to-first-byte criterion and 035 separately owns paired Portal/static-VirtioFS Git and package performance with accepted 2×/3× thresholds. | two-sample real diagnostic: candidate 3486/3521 ms, historical control 347/397 ms; 035 plan, accepted decision, current paired real gate, and semantic evaluator | upgrade 034 performance evidence to v2: 30 real per-run host-invocation-to-first-target-byte samples, representative attached workspace, 2-second p95, and a host monotonic clock; remove the stale pre-034 workload ratio while leaving 035's stricter metric-specific paired gate mandatory | resolved-local; exact replacement candidate and real Gate 2 pending |
| F-019 | The documented 034 quickstart rejected the new valid real isolation artifact before reaching its v2 performance check because it still required exactly 16 boolean checks while the producer and semantic evaluator require 26. | clean `ec755db` real Gate 2 passed with all 26 named checks and 30 performance samples; `scripts/test-concurrent-sessions-quickstart.sh --evidence-dir ... --require-real` stopped at the stale `length == 16` assertion | quickstart now requires the exact sorted 26-name inventory plus all values true, so arbitrary same-count keys and incomplete historical inventories both fail | resolved-local; final clean-commit rerun pending |
| F-020 | The exact signed candidate reported a successful safe VS Code handoff, but its run-qualified `--user-data-dir` was deep enough that VS Code's `1.12-main.sock` exceeded Darwin's 103-byte Unix-socket limit; Electron started, then exited with `listen EINVAL`, so 036 could not prove the host application survived VM stop. The same layout leaves only nine bytes of username/path headroom in an ordinary default store. | workflow `30203803506`, package SHA-256 `6161c4b6b15a19d31ddc5eb9cb31f5eddb1578b625741fac9c616be37e05b4df`; full candidate failed with `host application did not survive VM stop`; `.hideout-release-evidence/044-f020-full-lifecycle-probe` then passed the real retained-state/host-handoff, PTY recovery, 100-race, restart, and performance lanes | move run-qualified safe app state to compact store-owned `<store>/ha` while retaining 0700, store-global run isolation, and independence from session cleanup; declare the VS Code IPC suffix/103-byte limit in the strict Core safety profile and reject an overlong effect before launch | resolved-real-probe; exact replacement candidate pending |
| F-021 | The standalone 036 real Gate 2 probe still could not prove F-020 because its explicitly named short-root helper inherited macOS's long per-user `TMPDIR`; the new strict prelaunch guard correctly refused that overlong store. | `.hideout-release-evidence/044-f020-lifecycle-probe/logs/host-app.err` records the bounded refusal below `/var/folders/...`; `.hideout-release-evidence/044-f020-full-lifecycle-probe` passes from the independent short root | default the 036 short root independently to `/tmp`, retain an explicit `HIDEOUT_036_SHORT_TMPDIR` override, and make the release contract reject a return to ambient `TMPDIR` | resolved-real-probe; exact replacement candidate pending |
| F-022 | The 036 paired performance gate mounted one physical Git fixture into the pre-036 static-VirtioFS baseline and the current Portal candidate at the same time. Every AB sample made the following candidate take about 6.1 seconds while every BA candidate sample stayed near 0.53 seconds, so the result included cross-transport workspace interference. | `.hideout-release-evidence/044-f020-performance-probe/logs/performance-{candidate,baseline}-ms.txt` records the deterministic alternating pattern; both new 30-sample independent-workspace probes remove the alternation and retain matching fixture digests | use separate physical candidate/baseline workspaces and require identical tree-and-diff fixture digests before and after measurement; release contract forbids restoring one shared workspace | resolved-real-probe; exact replacement candidate pending |
| F-023 | Even after removing F-022's contamination, the current aggregate gate required every later release to re-pass 036 SC-010 against the pre-036 command path. That attributes the intentional 035 Portal transport and all later security/readiness work to the lifecycle-registry migration, contradicting 036's feature-specific wording and 040's explicit baseline-ownership decision. | 036's promoted clean proof at `0fe099f20e354d5d52187d9c8d5406a367d19d52` measured 170.511 ms versus 271.180 ms at pre-036; `.hideout-release-evidence/044-f020-full-lifecycle-probe` measures current p95 1187.302 ms with 30/30 samples within two seconds against exact pre-040 `322c3c6` | retain `036.lifecycle.real-gate2.performance` as validator-backed supporting historical evidence, keep current 036 lifecycle behavior fresh and exact-package, and make the aggregate current lane emit fresh 040 lifecycle/performance against exact pre-040 `322c3c6`; a new independent 040 semantic validator recomputes sample counts, median, p95, two-second coverage, baseline identity, workspace isolation, commit, package, and runtime | resolved-real-probe; aggregate exact-package Gate 2 pending |
| F-024 | The replacement signed candidate passed clean install and aggregate real Gate 2, then the runtime mutable-guest drift fixture intermittently failed while changing mode on the newly shadowed `~/.local/bin/git` inode. The claim under test is unprivileged PATH shadowing and runtime drift detection, not guest `chmod` semantics on a profile-home VirtioFS inode. | workflow `30208997784`, package SHA-256 `c8dbcb9dbd5647b555d5a135abc2e65c7c641a431ecd547547558a8b531cd37e`; `.hideout-release-evidence/044-alpha2-exact-candidate-3ef6267/runtime-gate2.err` records `runtime_durable_prefix=passed` followed by `chmod ... Operation not permitted`; `.hideout-release-evidence/044-runtime-drift-diagnostic/run-symlink` then passes the core real Gate 2, durable-prefix, and mutable-drift proofs with the exact signed binary and runtime provenance | shadow the first baseline command with a relative symlink to the already-proven executable that survived stop/start; this remains an unprivileged mutable-PATH drift, removes unrelated post-create chmod ownership timing, and is contract-guarded against restoring the old executable mutation | resolved-real-probe; exact replacement candidate pending |
| F-025 | The next signed candidate completed signing and notarization, but its final Gate 0 package rerun intermittently treated the deliberate PTY master close after native-harness target exit as a product failure. The close is required to keep completion bounded when another process inherits the slave; macOS may wake the concurrent reader with an `os.ErrClosed`-wrapping `PathError`, while the implementation only normalized the equivalent EIO and closed-pipe results. | workflow `30211433185`, job `89818208409` failed `TestRunWithStreamsAllocatesPTYWithInitialSize` with `read /dev/ptmx: file already closed`; the exact test then passed 1000 consecutive local repetitions, and the correction passed another 1000 repetitions, the package race suite, and full Gate 0 including its final native-package rerun | preserve the immediate post-wait close and normalize only errors identifiable through `errors.Is` as EIO, closed pipe, or `os.ErrClosed`; retain all unexpected PTY copy errors and guard both branches with a direct wrapped-error regression test | resolved-local; exact replacement candidate pending |
| F-026 | The exact signed candidate's projection-readiness warm lane intermittently returned a truncated or empty broker response before any host effect. The installed network service owns mutable state below `<service>/network`, but Lima reuse verification and DNS reconfiguration read and rewrote the parent service directory; every healthy warm session was therefore misclassified three times and unnecessarily rebuilt the TUN/DNS path. Removing this defect reduced the race surface but did not alone eliminate F-027, so it is not claimed as the sole root cause. | exact signed package SHA-256 `b18a3dc53bac49f6f76a085882742a93dd7107747b34cf4d3cfe8e82749fd4dc`; `.hideout-release-evidence/044-projection-warm-diagnostic-2/logs/failed-capture/failure-warm-4/session-audit.jsonl` records three failed network checks, successful self-heal, zero gateway connections, no host-app event, and broker EOF; the new path assertion failed against the old command before the fix | keep the helper executable at the service root while all PID/log/resolver/rollback state uses the actual nested network directory; emit bounded counter-only broker transport observations; require zero network verification failures for normal readiness samples and exactly three only in an opt-in forced-self-heal negative fixture | resolved-real-probe; exact replacement candidate pending |
| F-027 | After F-026, another 30-sample real run reached warm 12 with a valid readiness commit but the broker accepted zero connections and the shim decoded a non-JSON response. The reuse verifier checked only TUN/default route/PIDs/DNS; it never revalidated the established `host.lima.internal` control-plane bypass, so a missing or TUN-routed host route could be accepted as healthy and send broker traffic to an unrelated endpoint. | `.hideout-release-evidence/044-normal-network-reuse-diagnostic-9/logs/failed-capture/failure-warm-12/session-audit.jsonl` records successful network setup observations, readiness in 7 ms, broker counters all zero, no host effect, and `invalid character 'H'`; the new bypass assertion failed against the old verifier | re-read every root-owned `local-bypass-*-route.after` identity during warm verification, require a current route for each, and reject any route through `hideout0` before target commit; `.hideout-release-evidence/044-normal-network-reuse-diagnostic-10` then passed 1 fresh + 30 warm + cancellation with every warm at 7–10 ms and all retry/fallback/timeout/effect-leak columns zero | resolved-real-probe; exact replacement candidate pending |
| F-028 | The exact signed candidate verified its package-owned `tun2socks` helper, then Gate 3 copied only the main `hideout` executable into a temporary bin directory. Runtime helper resolution correctly searched beside the executing binary, found no manifest-bound helper there, and failed closed before guest routing. | workflow `30217822392`, exact package SHA-256 `5d0ec17acc6d1cf53d2d3b3048b5e3f4c28d4fc5c750f7b8ef3c634ff4b3f677`; `.hideout-release-evidence/044-alpha2-exact-candidate-2c2ecae/verification/phase1.out` records the failure, while `.hideout-release-evidence/044-f028-gate3-diagnostic-mapped/runtime-gate3/product-hardening-evidence.json` records the same package completing mediated DNS, HTTPS, projection alias privacy, and gateway forwarding with its adjacent helper | release Gate 3 executes the original package binary read-only so `os.Executable` retains helper/manifest adjacency; the public-release contract rejects reintroducing a main-binary-only copy | resolved-real-probe; exact replacement candidate pending |
| F-029 | Gate 4 created its symlink and FIFO inside the host workspace, then passed those host-absolute paths to a target whose visible workspace root is `/workspace`. The broker safely denied both at the guest-root boundary, so the assertions never exercised symlink resolution or special-file rejection and the stale symlink wording hid this false coverage. | `.hideout-release-evidence/044-alpha2-exact-candidate-2c2ecae/verification/phase1.out` stops after `verifying unsafe file target denial`; the bounded rerun reported `is outside workspace`, and `.hideout-release-evidence/044-f029-f030-gate4-diagnostic-2/gate4.out` records the corrected guest-visible fixtures passing the full real-browser lane | pass guest-visible `/workspace/...` paths (including file-URL fixtures), restore the specific `resolves outside workspace` and `not a regular file or directory` assertions, reject host-path fixtures in the release contract, and print bounded denial stderr on mismatch | resolved-real-probe; exact replacement candidate pending |
| F-030 | Standalone Gate 4 inherited macOS's long per-user `TMPDIR`; its daemon socket reached 104 bytes and failed before the browser boundary, even though the public candidate happened to supply a short parent. | direct real-browser rerun failed at `/var/folders/.../hideout-gate4.*/home/.hideout/daemon/hideoutd.sock`; `.hideout-release-evidence/044-f029-f030-gate4-diagnostic-2/gate4.out` records the standalone real-browser lane passing from its owned short root | Gate 4 owns a Darwin `/tmp` default with an explicit absolute non-root override; release-dogfood cleanup inventories both its ambient and short roots | resolved-real-probe; exact replacement candidate pending |
| F-031 | The exact candidate passed real Gate 3, projection alias privacy, gateway forwarding, and real-browser Gate 4, but the retained public `runtime-gate3.out` producer copied the bounded network/agent streams without re-emitting the separately verified alias and gateway markers. The strict privacy promoter correctly refused to infer those claims from the aggregate console log. | workflow `30220894156`, exact package SHA-256 `ffd00cbd0b944fcf5e2167c9346f86860f39300dcc3d282fa1f735f15e998b11`; `.hideout-release-evidence/044-alpha2-exact-candidate-cf7dbd5/verification/phase1.out` contains both `projection_alias_gate3=passed`, `gateway_forward_path=passed`, `gate3: passed`, and `gate4: passed`, while the original digest-bound runtime log omits only the first two and promotion fails closed; `.hideout-release-evidence/044-f031-gate3-diagnostic` reruns the same signed package through real Gate 3 and records both markers in a digest-matching public runtime log | after both checks pass, explicitly emit both bounded markers into the digest-bound public runtime log; keep the promoter strict and add a Gate 0 producer-template contract so a console-only marker cannot recur | resolved-real-probe; exact replacement candidate pending |
| F-032 | The next exact candidate passed clean install but aggregate Gate 2 intermittently failed ordinary sequential projections with broker EOF or timeout; a third candidate-shaped rerun separately captured a lifecycle defect: the 15-second idle timer had already invoked automatic stop, and the next two-phase attach returned `lifecycle stop is in flight` instead of absorbing the sub-second control-plane transition. Removing that direct error did not alone explain all EOF failures. | exact workflow `30223680363`, package SHA-256 `f763dc6f9a7f79835ac0f9dfe6b9a031be0086fe48182ae064bd36eaec2b7c52`, failed wrapper attempts under `.hideout-release-evidence/044-alpha2-exact-candidate-cb62231/{verification,verification-2}`, and `.hideout-release-evidence/044-f032-gate2-candidate-shape-3/gate2.err`; the retained journal commits the matching stop about 311 ms after it starts; after the lifecycle fix one full source Gate 2 passed, but `.hideout-release-evidence/044-f032-fixed-gate2-2` again failed safe projection with EOF | the confirmed lifecycle sub-defect now uses an in-memory completion fence: two-phase reservation waits at most 36 seconds, emits only bounded `automatic-stop-pending`, then takes a fresh backend observation after proved stop; cancellation and unknown stop results remain fail-closed, and the one-shot API keeps stale-observation protection. F-033 makes any pre-effect transport loss observable and safely retryable without repeating an effect. Two post-F-033 full real Gate 2 runs plus one package-shaped full Gate 2 with 1 fresh and 30 warm sessions passed without EOF, timeout, or stop-in-flight. The historical EOF packet origin is intentionally not overclaimed | resolved-real-probe; exact replacement candidate pending |
| F-033 | A projected host-app target proved guest files ready but did not authenticate the guest-to-host broker path before submitting an effect. Transport loss therefore surfaced raw `EOF`/TCP text, while the 20-second client/server request budget was shorter than one allowed 30-second macOS identity step; the launch guard also did not re-check cancellation after slow trust work, allowing a delayed host effect to cross an already-lost response boundary. | F-027 proves a ready first target with broker `accepted=0`; F-032 records residual EOF/timeout. Deterministic red tests showed a guard-cancelled `ExecLauncher` returned success and created its marker, three 45 ms identity steps escaped a 100 ms operation budget, the readiness action was absent, and broker/shim still exposed the old 20-second budget | before any host-app effect, the shim performs a session-token-authenticated, field-empty readiness request and retries only that no-effect probe for at most two seconds. Identity observation now inherits request cancellation and shares one 30-second total; deferred resolution plus final revalidation fit inside nested 65-second operation, 70-second response, and 75-second client budgets. The launcher re-checks cancellation after the guard, and response loss says the app may already be open instead of leaking raw transport text or claiming no effect. Gate 2 requires exact readiness/effect alternation and at least two accepted, parsed, and written broker messages per effect | resolved-real-probe; exact replacement candidate pending |
| F-034 | The exact candidate completed every security and UI lane before ordinary-user setup, but that final fixture nested `hideout-ordinary-user-release.*` and `setup-store-interrupt` below an already long release root. The interrupted setup daemon socket reached 105 bytes and the product correctly refused it on macOS. | final candidate attempt from clean `b54c668`; failure path `/private/tmp/hpa.T07QHR/t/hideout-ordinary-user-release.v2KQwO/setup-store-interrupt/daemon/hideoutd.sock`; all earlier exact-package Gate 2/3/4, lifecycle, first-run, privacy promotion, and forced UI assertions passed | shorten the fixture leaf to `hou.*` and its setup store to `s`; make the public-release contract reject the prior long forms. Local-fast ordinary-user acceptance, a real package setup PTY with a 61-byte interrupted socket, and full Gate 0 then passed | resolved-local-and-real-setup-probe; exact replacement candidate pending |
| F-035 | After F-034, the exact signed/notarized candidate reached workspace-executable performance after passing clean install, projection readiness, concurrent/shared-workspace/lifecycle gates, and first run. Its direct median remained faster than control (`0.960774x`), but host p95 rose to 2547.459 ms because dozens of historical failed-gate daemons were still alive. Investigation found that once an orphan's deleted runtime reached credential expiry, the rotation worker retried against the same expired deadline every millisecond and appended to an unlinked audit file indefinitely. | workflow `30242909403`, package SHA-256 `761b07e1c5155481794850c9b079da3ad7ac2ef0e5505c9ee697176291c3ddd4`; failed workspace lane measured direct median 1505.7915 ms, control median 1567.269 ms, ratio `0.960774`, and p95 2547.459 ms versus prior exact-candidate p95 944–1664 ms. Three sampled orphan audit descriptors were 4.7, 5.5, and 11.3 GB and growing about 180 KB/s; a bounded 50 ms-TTL reproduction against the exact package added 169,264 bytes in one second, all `daemon.credential.rotate` denials about 1 ms apart | bind credential rotation to the original runtime-directory inode; poll that identity and perform ordered self-stop when the runtime disappears or is replaced; apply 1-second-to-1-minute exponential backoff to other transient rotation failures. Deterministic unit tests cover replacement refusal, runtime-loss self-stop with a one-hour TTL, and single-event retry backoff; an external rebuilt-binary probe emitted one typed runtime-unavailable denial followed by `daemon.stop`. Historical test daemons/SOCKS fixtures and three deleted-root Lima hostagents were precisely removed while retaining the approved release proxy | resolved-local-and-fault-injection; exact replacement candidate pending |

## Requirement Evidence

Evidence values are `missing`, `contradicted`, `weak`, `passed-local`,
`passed-real`, or `passed-public`.

| Requirement group | Required evidence | Current evidence | Status |
| --- | --- | --- | --- |
| FR-001–FR-004 first result | exact-package install/setup/real Lima run | existing 038 evidence predates candidate | weak |
| FR-005–FR-007 help | CLI contract, zero-write checks, inventory mutation | focused CLI tests, operator-smoke judge, mutation M-002 | passed-local |
| FR-008–FR-013 doctor/recovery | summary unit/CLI tests, mutation, full-report parity | focused projection/CLI tests and existing typed fix tests | passed-local |
| FR-014–FR-018 support report | schema, redaction, safe-write, size, negative fixtures | strict schema/unit/CLI tests and local-fast adversarial runner | passed-local |
| FR-019–FR-024 privacy helper | package/helper manifests, license, resolver proof, Gate 3 | package smoke and focused resolution tests pass; exact-package real Gate 3 still pending | passed-local |
| FR-025–FR-027 lifecycle | exact-candidate upgrade/repair/uninstall/purge | local prior-version/helper-addition, repair, uninstall, purge, scope, and guidance proof passes; exact candidate pending | passed-local |
| FR-028–FR-035 release | aggregate, real gates, UI, signing, notarization, receipt | none for 044 candidate | missing |

## Requirement And Success-Criterion Audit

This audit distinguishes implemented local behavior from evidence that can only
exist after freezing or publishing one exact candidate.

| IDs | Authoritative evidence reviewed | Result |
| --- | --- | --- |
| FR-001–FR-004 | audience/docs, package install, setup PTY, first-run local and retained pre-044 real evidence | passed-local; new exact candidate pending |
| FR-005–FR-007 | help unit/CLI inventory, contextual zero-write checks, M-002/N-001 | passed-local |
| FR-008–FR-013 | doctor projection/full-report tests, repair tasks, M-003/N-002, install and first-run smokes | passed-local |
| FR-014–FR-018 | support-report schema/model/write/CLI tests and injected-secret local aggregate | passed-local |
| FR-019–FR-024 | pinned helper module/license/manifests, resolver/package tests, package-only Gate 3 verification, M-005/N-005 | passed-local; exact-package real Gate 3 pending |
| FR-025–FR-027 | Homebrew/standalone help, upgrade/repair/uninstall/purge fixtures, scope mutation M-006 | passed-local |
| FR-028 | exact package ordinary-user journey | runner and rejection contract passed-local; exact clean candidate run pending |
| FR-029 | mutation and negative-fixture discipline | M-001–M-012 and N-001–N-009 passed-local |
| FR-030 | candidate coverage aggregation | registry, automatic all-proof candidate runner, Gate 0 and readiness integration passed-local; candidate artifact pending |
| FR-031 | dirty/private/unpushed/rebuilt/stale/signing/notary/download rejection | N-007 plus readiness/releasechannel judges passed-local |
| FR-032–FR-035 | docs truth, prerelease workflow contracts, debt review, adversarial ledger | passed-local; receipt-derived public rendering pending |
| SC-001–SC-002 | supported-Mac first useful result and time bound | pending new exact-package Gate 2 measurements |
| SC-003–SC-004 | primary/contextual help bounds and coverage | passed-local |
| SC-005–SC-006 | concise healthy/blocked doctor and runnable safe actions | passed-local |
| SC-007 | one bounded schema-valid support artifact with zero injected protected material | passed-local |
| SC-008 | clean-host exact package verification plus real privacy gate | package verification passed-local; clean exact-package Gate 3 pending |
| SC-009 | helper removal/change/replacement/non-executable failure | passed-local; exact-package Gate 3 positive pending |
| SC-010 | durable/unrelated state preservation through upgrade and normal uninstall | passed-local |
| SC-011 | one exact package across all acceptance, gates, UI, signing, and notary | pending candidate freeze |
| SC-012 | all new mutations and judge negative fixtures | passed-local |
| SC-013 | English/Chinese docs, help, package guidance, support matrix, release notes | passed-local before publication; receipt rendering pending |
| SC-014 | candidate-created resource cleanup | local fixtures passed; exact-candidate cleanup inventory pending |

## Mutation Proofs

| ID | Assertion | Mutation | Red observation | Restored green | Status |
| --- | --- | --- | --- | --- | --- |
| M-001 | Primary next command is exact | Changed `git status --short` to `git status` in `writeSetupNextSteps` | `TestSetupNextStepsKeepsOrdinaryJourneyFirst` failed and printed the changed journey | Restored exact command; focused setup tests passed | passed-local |
| M-002 | Help inventory is complete | Removed the `hideout shim build-linux` entry from expanded help | `TestExpandedHelpRetainsCompleteCommandInventory` failed and named the missing entry | Restored entry; focused help tests passed | passed-local |
| M-003 | Doctor summary reflects readiness state | Changed the attention label from `Needs attention` to `Ready` | `TestDoctorDefaultIsConciseAndDetailedModesRetainFindings` failed and showed the false-ready output | Restored label; focused doctor tests passed | passed-local |
| M-004 | Support redaction rejects protected material | Removed proxy-value matching from the final redaction validator | `TestValidateRejectsOversizeAndProtectedMaterial/proxy` failed because the injected credentialed SOCKS URL was accepted | Restored the proxy validator; support-report package and local-fast acceptance passed | passed-local |
| M-005 | Package helper digest is enforced | Removed the manifest-digest comparison from `Tun2SocksHelperCurrent` | `TestResolveLinuxTun2SocksIgnoresAmbientPathAndRejectsDigestDrift` failed because the drifted store helper resolved | Restored comparison; focused resolver tests and package smoke passed | passed-local |
| M-006 | Rejected uninstall performs no partial deletion | Disabled whole-file-scope prevalidation before the deletion loop | `TestRepairAndUninstallRejectSymlinkedObsoleteAncestors/uninstall` failed because `bin/hideout` had already been removed before the poisoned path was rejected | Restored full prevalidation; focused package tests and recovery E2E passed | passed-local |
| M-007 | Release aggregate rejects mismatched candidate identity | Removed the package-commit-to-checkout-`HEAD` comparison from `validate_release_candidate_source` | `scripts/test-release-readiness.sh --negative-fixtures` failed with `stale package commit fixture unexpectedly passed` | Restored the comparison; the complete negative suite passed | passed-local |
| M-008 | Registry requires every 044 proof | Removed the public-receipt registry row | `TestOrdinaryUserReleaseRegistryCoversTargetedRealAndPublicProofs` failed with `requirements=6 want=7` | Restored row; `go test ./internal/productevidence` passed | passed-local |
| M-009 | Network reuse reads the installed state directory | Evaluated the new exact-path assertions against the previous parent-directory verifier and DNS reconfigure command | `TestEnvironmentNetworkReuseVerificationChecksCurrentBootAndRuntimeHealth` and `TestEnvironmentNetworkDNSReconfigureSeparatesHelperAndStateDirectories` failed, naming the absent nested paths | Corrected the verifier, direct-network stale check, DNS state, and rollback verifier; focused Lima tests passed | passed-local |
| M-010 | Network reuse revalidates every persisted local bypass | Evaluated route-file/current-route assertions against the previous TUN/PID/DNS-only verifier | `TestEnvironmentNetworkReuseVerificationChecksCurrentBootAndRuntimeHealth` failed and named the absent `local-bypass-*-route.after` loop | Added non-empty/current/non-`hideout0` checks; the focused test then passed 100 consecutive repetitions | passed-local |
| M-011 | A cancelled request cannot cross the final host launch boundary | Ran the post-guard cancellation fixture against the launcher before the final context check existed | `TestExecLauncherRefusesEffectWhenGuardConsumesRequestDeadline` returned success and `/usr/bin/touch` created the forbidden marker | Added the final cancellation fence; the test returns `context.Canceled` and the marker remains absent | passed-local |
| M-012 | macOS identity verification consumes one total operation budget | Ran three simulated 45 ms signing/trust steps under a 100 ms budget against the previous per-command timeouts | `TestObserveDarwinSigningIdentityUsesOneBoundedOperationBudget` completed all 135 ms of work and incorrectly succeeded | Bound every command to one inherited operation context; the test now times out within the total budget | passed-local |

## Negative Fixtures

| ID | Judge | Fixture | Observed result | Status |
| --- | --- | --- | --- | --- |
| N-001 | Help contextual routing | unknown topic plus an expanded-help fixture with the shim entry removed | CLI rejects the unknown topic with `help --all`; the operator-smoke inventory judge rejects the incomplete fixture | passed-local |
| N-002 | Doctor ordinary-action filter | warning whose sole next action is `scripts/test-gate3-hidden-proxy.sh` | projection remained ready, omitted the finding, and rendered no `scripts/test-*` text; mixed safe/maintainer fixture retained only `hideout daemon start` | passed-local |
| N-003 | Support output validator | symlink, overwrite, unsafe parent, oversize | every fixture was rejected before a final file was retained; atomic success mode is `0600` | passed-local |
| N-004 | Support redaction validator | injected capability/UI token, credentialed proxy, secret backing name, home/workspace path, machine ID, workspace sentinel, and oversize body | unit validators rejected direct injections; the local-fast real CLI report contained none of the injected values or workspace content | passed-local |
| N-005 | Helper provenance validator | missing, digest-damaged, wrong target, invalid release override, invalid development override, and ambient `PATH` helper | installer and Gate 3 validator rejected missing/damaged/wrong-target package helpers; release Gate 3 rejected the override; Go resolver rejected the invalid development override without falling through and ignored ambient `PATH` | passed-local |
| N-006 | Package lifecycle scope | prior install without package helper, unrelated prefix/store files, poisoned out-of-root obsolete path, purge without confirmation | candidate added helper/license while preserving state; repair and uninstall preserved unrelated/durable files; poisoned scope failed before deleting `bin/hideout`; unconfirmed purge failed with store intact | passed-local |
| N-007 | Release candidate validator | dirty checkout, unpushed commit, stale package commit, changed package bytes, stale product digest, missing/failed Gate 2 or Gate 3, native gate substitution, missing 044 UI, invalid signing/notarization, and anonymous-download digest drift | `scripts/test-release-readiness.sh --negative-fixtures` rejected every source fixture and ran the focused releasecompat, releasechannel, and productevidence judges; M-007 proved the stale-commit fixture fires | passed-local |
| N-008 | Projection readiness network reuse judge | remove the actual nested `tun2socks.pid` immediately before one selected warm sample in the disposable gate-owned VM | the selected sample recorded exactly three failed verification attempts, one successful self-heal, one projected first target, one launched host effect, and a successful redacted broker transport observation; all retry/fallback/timeout/effect-leak columns remained zero; the isolated wrapper returned success | passed-real-probe |
| N-009 | Broker readiness no-effect envelope | wrong session token and individually injected subject, command, argv, route, or args fields | the broker denied unauthorized probes, rejected every effect-bearing probe as `bad-request`, and invoked neither host URL nor file opener | passed-local |

## Verification Commands

Commands are added only after they have actually run. Planned commands live in
`quickstart.md`; this section records observations rather than intentions.

| Command | Candidate identity | Result | Artifact |
| --- | --- | --- | --- |
| pending | not frozen | not run | none |
| `go test ./internal/productevidence` | pre-candidate working tree | passed | local test output, 2026-07-26 |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-scaffold.json` | pre-candidate working tree | scaffold passed | `/tmp/hideout-044-scaffold.json` |
| `go test ./internal/app -run 'Test(PrimaryHelp\|ExpandedHelp\|ContextualHelp\|HelpRejects\|UsageGroups\|SetupRejectsEveryNonHelpArgument)' -count=1` | pre-candidate working tree | passed after observed M-002 red mutation | local test output, 2026-07-26 |
| `go test ./internal/doctor ./internal/app -run 'Test(ReadinessSummary\|DoctorDefaultIsConcise\|DoctorJSONRemainsComplete\|DoctorConciseBlocked)' -count=1` | pre-candidate working tree | passed after observed M-003 red mutation | local test output, 2026-07-26 |
| `go test ./internal/supportreport ./internal/app -count=1` | pre-candidate working tree | passed after observed M-004 red mutation | local test output, 2026-07-26 |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-local-fast.json` | pre-candidate working tree | passed with real CLI redaction/safe-write fixtures | `/tmp/hideout-044-local-fast.json` |
| `go test ./internal/helperbin ./internal/packagekit ./internal/supportreport ./internal/manager ./internal/app` | pre-candidate working tree | passed after replacing the stale external-prerequisite assertion | local test output, 2026-07-26 |
| `scripts/test-package-smoke.sh` | pre-candidate working tree | passed with package-owned helper, license, ambient-path, override, digest, target, install, repair, and uninstall assertions | local test output, 2026-07-26 |
| `cmp third_party/tun2socks/LICENSE <v2.6.0-module>/LICENSE` plus readonly module identity check | pinned `github.com/xjasonlyu/tun2socks/v2@v2.6.0` | exact upstream MIT license matched; module and sums remained pinned | local review output, 2026-07-26 |
| `scripts/test-doctor-package-recovery-e2e.sh --local-fast --out /tmp/hideout-044-recovery-2` | pre-candidate working tree | passed with lifecycle scope, preserved-state, exact purge confirmation, repair, doctor, schema, and redaction evidence | `/tmp/hideout-044-recovery-2/product-hardening-evidence.json` |
| `scripts/test-ordinary-user-release.sh --local-fast --out /tmp/hideout-044-aggregate.json` | pre-candidate working tree | passed complete targeted 044 aggregation and `validate-044 --target targeted-completion` | `/tmp/hideout-044-aggregate.json` plus sibling artifacts |
| `scripts/test-release-readiness.sh --negative-fixtures` | source-fixture repositories plus unit release identities | passed after observed M-007 failure; rejected dirty/private/stale/rebuilt/missing-gate/UI/signing/notarization/download fixtures | transient test roots removed |
| `HIDEOUT_PHASE1_PRINT_PLAN=1 ... scripts/test-release-dogfood.sh` | pre-candidate package `28db82a36da2089a835472ec025c6023129212ef` | passed package-owned helper extraction/verification and release-candidate plan without external tun2socks path | transient `/tmp/hideout-044-dogfood-plan`, removed after review |
| `scripts/test-doc-truth-smoke.sh --out /tmp/hideout-044-doc-truth` | pre-candidate working tree | passed after ordinary-user, helper-supply, candidate, and public-boundary convergence | transient docs evidence |
| `go test ./internal/app ./internal/productevidence ./internal/releasecompat ./internal/releasechannel -count=1` | pre-candidate working tree | passed, including readiness-level refusal of missing 044 UI proof | local test output, 2026-07-26 |
| `go test -race ./internal/supportreport ./internal/helperbin ./internal/packagekit ./internal/productevidence ./internal/releasecompat -count=1` | pre-candidate working tree | passed | local race-test output, 2026-07-26 |
| `scripts/test-install-smoke.sh` plus both local first-run lanes | pre-candidate working tree | passed after F-009 concise/detailed doctor convergence | transient package/evidence roots |
| `scripts/test-gate0.sh` | pre-candidate working tree | passed complete static, unit, formal-model, package, product-smoke, docs, mutation, negative-fixture, first-run, and local UI battery | local console observation, 2026-07-26 |
| `scripts/test-public-alpha-release.sh --no-publish` plus `scripts/test-projection-readiness-smoke.sh` | pre-candidate working tree after F-010/F-011 | passed automatic producer wiring, privacy promotion contract, semantic false-green fixtures, and no-publish authority checks | local test output, 2026-07-26 |
| synthetic matching Gate 2/Gate 3 promotion with distinct environment IDs | retained historical clean 043 fixture plus generated Gate 3 fixture | `validate-043` accepted all seven 030/032/039/043 proofs; mismatched checks remain covered by the Go semantic negative suite | transient `/tmp/hideout-privacy-promote.*`, removed |
| `scripts/test-gate0.sh` | pre-candidate working tree after F-010/F-011 | passed complete Gate 0 including the new privacy promotion, exact-package lifecycle registry, release contract, docs truth, first-run, and UI checks | local console observation, 2026-07-26 |
| shortened real 034 diagnostic with 1 warm-up and 2 measured samples | pre-candidate F-018 working tree | passed three-session isolation, crash/PTY/recovery mechanics, v2 sampling, and cleanup; first-target-byte samples 168.863/182.186 ms | `.hideout-release-evidence/044-f018-probe` (diagnostic only; not release evidence) |
| `go test ./... -count=1`, `scripts/test-public-alpha-release.sh --no-publish`, and docs truth | pre-candidate F-018 working tree | passed strict v2 semantics, complete Go regression, release contracts, and docs truth | local output and ignored docs evidence, 2026-07-26 |
| `scripts/test-gate0.sh` | pre-candidate F-018 working tree | passed full static, unit, formal-model, package, product-smoke, docs, mutation, negative-fixture, first-run, and local UI battery | local console observation, 2026-07-26 |
| `go test -race ./internal/backend/lima ./internal/broker ./internal/manager -count=1`, focused 100-repeat suites, projection and host-capability smokes | pre-candidate F-026/F-027 working tree | passed path, transport-counter, redaction, race, and shell/product-contract regressions | local output, 2026-07-26 |
| readiness-only projection diagnostic, 1 fresh + 30 warm + cancellation | pre-candidate F-027 working tree | passed all 30 warm samples at 7–10 ms with projected first target and zero retry/fallback/timeout/unauthorized/cross-session counts | `.hideout-release-evidence/044-normal-network-reuse-diagnostic-10` (diagnostic only; not release evidence) |
| forced-self-heal readiness-only diagnostic, 1 fresh + 4 warm + cancellation, warm sample 2 injected | pre-candidate F-027 working tree | passed the isolated wrapper; the forced sample completed in 8 ms after exactly three expected verification failures and one self-heal, with no retry/fallback/timeout/effect leak | `.hideout-release-evidence/044-forced-self-heal-diagnostic-11` (diagnostic only; not release evidence) |
| `go test ./... -count=1` and `scripts/test-gate0.sh` | pre-candidate F-027 working tree | passed the complete Go suite and Gate 0 static/unit/race/formal/package/product/docs/first-run/forced-UI battery after the audit schema red test was corrected | local output, 2026-07-27 |
| full source-built runtime Gate 2 with package-equivalent short roots and external host-app pack | dirty F-032 working tree based on `cb622314024fa320d0822f466e8f12cf90252deb` | passed safe/HostFS/trusted/persistent/external projections, complete HostFS, lifecycle, and disposable convergence after the old candidate-shaped run captured `lifecycle stop is in flight` | `.hideout-release-evidence/044-f032-fixed-gate2-1` (diagnostic only; not release evidence) |
| second full source-built runtime Gate 2 with the same shape | same dirty F-032 working tree | failed safe code projection with EOF after the bounded lifecycle wait was present, disproving that the direct stop-in-flight correction resolved every residual transport failure | `.hideout-release-evidence/044-f032-fixed-gate2-2` (diagnostic only; not release evidence) |
| three further full source-built runtime Gate 2 repetitions with retained stores | same dirty F-032 lifecycle working tree before F-033 | all three passed every projection, HostFS, lifecycle, and disposable lane; useful stability diagnostics but not evidence for the later broker/timeout correction | `.hideout-release-evidence/044-f032-residual-gate2-{1,2}` and `.hideout-release-evidence/044-f032-residual-gate2-3.{out,err}` |
| `go test ./... -count=1` plus focused hostcap/appopen/broker/shim/Manager suites | dirty F-033 working tree based on `cb622314024fa320d0822f466e8f12cf90252deb` | passed after the deterministic launch-after-cancel, cumulative identity-budget, missing readiness action, and stale timeout red observations | local output, 2026-07-27 |
| first full source-built runtime Gate 2 after F-033 | same dirty F-033 working tree | passed every projection, HostFS, lifecycle, and disposable lane; retained audits contain one authenticated readiness for every host-app effect and healthy broker transport counters | `.hideout-release-evidence/044-f033-gate2-1.{out,err}` plus retained `/tmp/hd.3iXDgN` |
| second full source-built runtime Gate 2 with the new preflight judge enabled | same dirty F-033 working tree | passed; 13 effect-bearing sessions had equal readiness/effect counts, accepted/parsed/written counts of 2 per pair, and zero parse/write failures; bounded logs contained no EOF, timeout, reset, broken pipe, or stop-in-flight | `.hideout-release-evidence/044-f033-gate2-2.{out,err}` plus retained `/tmp/hd.jiST81` |
| package-shaped full runtime Gate 2 plus projection-readiness stress | package built from the same dirty F-033 working tree | passed the complete Gate 2 and 1 fresh + 30 warm + cancellation; every target was projected, warm readiness averaged 8.8 ms with 12 ms maximum, and retry/fallback/timeout/unauthorized/cross-session counts were all zero | `.hideout-release-evidence/044-f033-readiness-stress` (probe only; not release evidence) |
| `scripts/test-gate0.sh` | dirty F-033 working tree immediately before candidate freeze | passed complete Go, race, seven-model, package, host-app, readiness false-green, support/recovery, release-negative, docs-truth, first-run, UI, concurrency, and lifecycle battery | local console observation, 2026-07-27 |

## Cleanup Inventory

| Resource class | Created | Removed/accounted | Status |
| --- | --- | --- | --- |
| Test sessions | 0 | 0 | pending |
| Test environments | 0 | 0 | pending |
| Temporary stores | 0 | 0 | pending |
| Package prefixes | 0 | 0 | pending |
| Support reports | local-fast and unit-test temporary files | test temp roots removed; no user-store report residue | passed-local |
| Retained release evidence | local aggregate and docs/dogfood plan fixtures only | development fixtures removed or confined to `/tmp`; no candidate/public evidence claimed | passed-local |

## Completion Audit

044 may be marked implemented only when:

- every task is checked with linked evidence;
- every functional requirement and success criterion is passed at its required
  local, real, or public scope;
- all mutation and negative-fixture rows are green;
- exact-package Gate 2, Gate 3, and required UI evidence pass;
- the clean commit is public and CI is green;
- signing, notarization, anonymous download, and publication receipt validate;
- documentation and release inventory are rendered from the receipt; and
- cleanup has no unaccounted residue.
